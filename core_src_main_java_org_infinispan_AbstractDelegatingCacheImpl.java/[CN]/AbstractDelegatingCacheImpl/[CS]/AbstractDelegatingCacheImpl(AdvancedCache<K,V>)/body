{
  super(cache);
  if (cache instanceof CacheImpl)   this.cache=(CacheImpl<K,V>)cache;
 else   throw new IllegalArgumentException("This adapter only works with CacheImpl, not with " + cache.getClass().getSimpleName());
}
