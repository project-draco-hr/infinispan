{
  if (gtx != null)   pendingPrepares.remove(gtx);
  if (isActive() && modifications != null && modifications.size() > 0)   addEntry(gtx,modifications.toArray(new WriteCommand[modifications.size()]));
}
