{
  if (upsertRowSql == null) {
    upsertRowSql=String.format("%s ON DUPLICATE KEY UPDATE %s = ?, %s = ?",getInsertRowSql(),config.dataColumnName(),config.timestampColumnName());
  }
  return upsertRowSql;
}
