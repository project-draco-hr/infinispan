{
  assertEmpty(KEY_1);
  put(0,KEY_1,VALUE_1);
  assertCacheValue(KEY_1,VALUE_1);
  cache(0).clear();
  awaitClear(0);
  assertCacheValue(KEY_1,null);
  assertNoTransactions();
  assertNoTxStats();
}
