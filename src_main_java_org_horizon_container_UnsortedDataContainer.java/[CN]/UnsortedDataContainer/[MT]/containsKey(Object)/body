{
  if (!immortalData.containsKey(k)) {
    ExpirableCachedValue ecv=expirableData.get(k);
    if (ecv == null)     return false;
    if (ecv.isExpired()) {
      expire(k);
      return false;
    }
  }
  return true;
}
