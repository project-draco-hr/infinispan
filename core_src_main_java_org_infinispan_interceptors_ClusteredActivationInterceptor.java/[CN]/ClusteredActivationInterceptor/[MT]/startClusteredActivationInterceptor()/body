{
  CacheMode cacheMode=cacheConfiguration.clustering().cacheMode();
  isWriteSkewConfigured=cacheConfiguration.locking().writeSkewCheck() && (cacheMode.isReplicated() || cacheMode.isDistributed());
}
