{
switch (type) {
case INCR:
    return new IncrementCommand(cache,type,key,value);
case DECR:
  return new DecrementCommand(cache,type,key,value);
default :
throw new StreamCorruptedException("Unable to build storage command for type: " + type);
}
}
