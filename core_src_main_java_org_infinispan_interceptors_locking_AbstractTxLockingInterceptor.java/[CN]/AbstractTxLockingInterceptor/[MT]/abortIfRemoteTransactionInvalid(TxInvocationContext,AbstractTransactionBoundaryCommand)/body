{
  if (!ctx.isOriginLocal()) {
    Address origin=c.getGlobalTransaction().getAddress();
    if (!rpcManager.getTransport().getMembers().contains(origin))     throw new CacheException("Member " + origin + " no longer in cluster. Forcing tx rollback for "+ c.getGlobalTransaction());
  }
}
