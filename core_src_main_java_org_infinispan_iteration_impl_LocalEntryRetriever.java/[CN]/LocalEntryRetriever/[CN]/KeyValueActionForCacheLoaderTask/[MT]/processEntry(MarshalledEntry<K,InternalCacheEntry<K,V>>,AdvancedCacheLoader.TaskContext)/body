{
  if (!taskContext.isStopped()) {
    action.apply(marshalledEntry.getKey(),(InternalCacheEntry<K,V>)new ImmortalCacheEntry(marshalledEntry.getKey(),marshalledEntry.getValue()));
    if (Thread.interrupted()) {
      throw new InterruptedException();
    }
  }
}
