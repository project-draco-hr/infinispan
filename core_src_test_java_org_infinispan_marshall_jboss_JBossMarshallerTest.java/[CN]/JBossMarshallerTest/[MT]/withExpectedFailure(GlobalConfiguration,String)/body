{
  try {
    EmbeddedCacheManager cm=TestCacheManagerFactory.createCacheManager(globalCfg);
    cm.getCache();
    fail(message);
  }
 catch (  EmbeddedCacheManagerStartupException e) {
    if (e.getCause() instanceof ConfigurationException)     log.trace("Expected exception",e);
 else     throw e;
  }
}
