{
  FileOutputStream fos=null;
  try {
    if (bytes.length > 0) {
      fos=new FileOutputStream(f);
      fos.write(bytes);
      fos.flush();
    }
 else     if (f.exists()) {
      f.delete();
    }
  }
  finally {
    if (fos != null)     fos.close();
  }
}
