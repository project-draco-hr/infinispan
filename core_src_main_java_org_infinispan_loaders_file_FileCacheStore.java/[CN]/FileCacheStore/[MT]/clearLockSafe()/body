{
  File[] toDelete=root.listFiles(NUMERIC_NAMED_FILES_FILTER);
  if (toDelete == null) {
    return;
  }
  for (  File f : toDelete) {
    if (!deleteFile(f)) {
      log.problemsRemovingFile(f);
    }
  }
}
