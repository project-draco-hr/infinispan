{
  File[] toDelete=root.listFiles();
  if (toDelete == null) {
    return;
  }
  for (  File f : toDelete) {
    f.delete();
    if (f.exists())     log.warn("Had problems removing file {0}",f);
  }
}
