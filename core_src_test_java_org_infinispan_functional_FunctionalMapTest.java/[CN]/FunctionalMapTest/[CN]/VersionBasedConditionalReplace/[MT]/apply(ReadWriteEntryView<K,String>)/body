{
  EntryVersionParam<Long> versionParam=rw.getMetaParam(EntryVersionParam.ID());
  if (versionParam.get().compareTo(new NumericEntryVersion(version)) == EQUAL)   rw.set("uno",new EntryVersionParam<>(new NumericEntryVersion(200)));
  return rw;
}
