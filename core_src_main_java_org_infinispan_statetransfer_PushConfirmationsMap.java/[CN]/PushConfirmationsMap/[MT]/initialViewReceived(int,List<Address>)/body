{
  lock.lock();
  try {
    lastViewId=viewId;
    membersCount=members.size();
    actualConfirmationsCount=0;
    pushConfirmations.clear();
    joinConfirmations.clear();
  }
  finally {
    lock.unlock();
  }
}
