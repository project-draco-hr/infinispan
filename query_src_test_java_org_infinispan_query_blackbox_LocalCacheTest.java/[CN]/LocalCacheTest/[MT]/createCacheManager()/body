{
  Configuration c=getDefaultClusteredConfig(LOCAL,true);
  c.fluent().indexing().indexLocalOnly(false).addProperty("hibernate.search.default.directory_provider","ram");
  enhanceConfig(c);
  return TestCacheManagerFactory.createCacheManager(c,true);
}
