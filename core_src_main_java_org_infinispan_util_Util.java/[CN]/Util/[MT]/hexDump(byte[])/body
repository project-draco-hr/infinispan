{
  StringBuilder buf=new StringBuilder(buffer.length << 1);
  for (  byte b : buffer)   buf.append(HEX_VALUES.charAt((b & 0xF0) >> 4)).append(HEX_VALUES.charAt((b & 0x0F)));
  return buf.toString();
}
