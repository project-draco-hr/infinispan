{
  RpcOptions options=getRpcOptionsBuilder(mode,usePriorityQueue ? DeliverOrder.NONE : DeliverOrder.PER_SENDER).timeout(timeout,TimeUnit.MILLISECONDS).responseFilter(responseFilter).build();
  return invokeRemotely(recipients,rpcCommand,options);
}
