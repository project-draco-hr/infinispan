{
  RpcOptions options=getRpcOptionsBuilder(mode,!usePriorityQueue).timeout(timeout,TimeUnit.MILLISECONDS).responseFilter(responseFilter).build();
  return invokeRemotely(recipients,rpcCommand,options);
}
