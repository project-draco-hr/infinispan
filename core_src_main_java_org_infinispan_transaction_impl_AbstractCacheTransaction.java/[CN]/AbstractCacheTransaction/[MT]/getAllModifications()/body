{
  if (modifications instanceof ImmutableListCopy)   return modifications;
 else   if (modifications == null)   return Collections.<WriteCommand>emptyList();
 else   return Immutables.immutableListCopy(modifications);
}
