{
  QueryFactory qf=Search.getQueryFactory(cache);
  Query q=qf.from(User.class).orderBy("id",SortOrder.ASC).maxResults(5).build();
  List<User> list=q.list();
  assertEquals(3,q.getResultSize());
  assertEquals(3,list.size());
}
