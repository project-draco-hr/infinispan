{
  QueryFactory qf=getQueryFactory();
  Query q=qf.from(User.class).orderBy("id",SortOrder.ASC).maxResults(0).build();
  List<User> list=q.list();
  assertEquals(3,q.getResultSize());
  assertEquals(0,list.size());
}
