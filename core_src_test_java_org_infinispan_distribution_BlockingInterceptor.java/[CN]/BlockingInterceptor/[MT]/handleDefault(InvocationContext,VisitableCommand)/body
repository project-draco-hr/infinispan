{
  try {
    if (!blockAfter) {
      blockIfNeeded(ctx,command);
    }
    return super.handleDefault(ctx,command);
  }
  finally {
    if (blockAfter) {
      blockIfNeeded(ctx,command);
    }
  }
}
