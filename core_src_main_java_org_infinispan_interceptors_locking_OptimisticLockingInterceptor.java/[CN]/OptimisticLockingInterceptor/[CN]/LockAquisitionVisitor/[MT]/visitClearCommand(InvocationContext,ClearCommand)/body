{
  for (  Object key : dataContainer.keySet()) {
    lockKey(ctx,key);
    if (!ctx.isOriginLocal())     entryFactory.wrapEntryForClear(ctx,key);
  }
  if (!ctx.isOriginLocal())   invokeNextInterceptor(ctx,command);
  return null;
}
