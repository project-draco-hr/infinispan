{
  for (  Object key : command.getMap().keySet()) {
    if (cll.localNodeIsOwner(key)) {
      lockKey(ctx,key);
      if (!ctx.isOriginLocal())       entryFactory.wrapEntryForPut(ctx,key,true);
    }
  }
  if (!ctx.isOriginLocal())   invokeNextInterceptor(ctx,command);
  return null;
}
