{
  return new KeyValueFilter<Integer,String>(){
    @Override public boolean accept(    Integer key,    String value,    Metadata metadata){
      if (!dynamic && key.equals(2))       return true;
      if (dynamic && params[0].equals(key))       return true;
      return false;
    }
  }
;
}
