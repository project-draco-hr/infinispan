{
  Cache<K,V> cache=defaultJCacheManager.getCache(cacheName);
  if (cache != null)   return cache;
  return defaultJCacheManager.configureCache(cacheName,defaultCacheManager.<K,V>getCache().getAdvancedCache());
}
