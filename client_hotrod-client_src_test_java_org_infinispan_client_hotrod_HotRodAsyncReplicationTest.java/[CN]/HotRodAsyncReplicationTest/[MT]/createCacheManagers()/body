{
  Configuration cfg=new Configuration().fluent().clustering().mode(Configuration.CacheMode.REPL_ASYNC).async().replQueueInterval(1000L).useReplQueue(true).eviction().maxEntries(3).build();
  createHotRodServers(2,cfg);
}
