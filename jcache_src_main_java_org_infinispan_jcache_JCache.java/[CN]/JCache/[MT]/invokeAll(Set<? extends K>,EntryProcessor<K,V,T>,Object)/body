{
  checkNotClosed();
  verifyKeys(keys);
  verifyEntryProcessor(entryProcessor);
  Map<K,T> result=new HashMap<K,T>(keys.size());
  for (  K key : keys) {
    T t=invoke(key,entryProcessor,arguments);
    if (t != null)     result.put(key,t);
  }
  return result;
}
