{
  if (log.isTraceEnabled())   log.trace("Substract {0} to {1}",newValue,oldValue);
  BigInteger b=oldValue.subtract(newValue);
  if (b.signum() < 0)   return BigInteger.valueOf(0);
 else   return b;
}
