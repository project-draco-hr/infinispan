{
  if (!lookupDone)   doLookups(configuration.classLoader());
  if (tm != null)   return tm;
  if (lookupFailed) {
    if (!noJBossTM) {
      tryEmbeddedJBossTM();
    }
    if (noJBossTM) {
      useDummyTM();
    }
  }
  return tm;
}
