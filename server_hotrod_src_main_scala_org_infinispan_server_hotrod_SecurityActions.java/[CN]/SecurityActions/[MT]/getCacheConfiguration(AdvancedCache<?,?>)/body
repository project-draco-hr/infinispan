{
  if (System.getSecurityManager() != null) {
    return AccessController.doPrivileged(new PrivilegedAction<Configuration>(){
      @Override public Configuration run(){
        return cache.getCacheConfiguration();
      }
    }
);
  }
 else {
    return cache.getCacheConfiguration();
  }
}
