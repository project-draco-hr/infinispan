{
  Configuration config=getDefaultStandaloneConfig(false).fluent().loaders().addCacheLoader(new SlowCacheStoreConfig().storeName(this.getClass().getName()).asyncStore().threadPoolSize(1).build()).build();
  return TestCacheManagerFactory.createCacheManager(config);
}
