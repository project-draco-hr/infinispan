{
  ConfigurationBuilder config=getDefaultStandaloneCacheConfig(false);
  config.loaders().addStore(DummyInMemoryCacheStoreConfigurationBuilder.class).storeName(this.getClass().getName()).slow(true).async().enable().threadPoolSize(1).build();
  return TestCacheManagerFactory.createCacheManager(config);
}
