{
  Iterator<V> e=iterator();
  if (o == null) {
    while (e.hasNext()) {
      if (e.next() == null) {
        e.remove();
        return true;
      }
    }
  }
 else {
    while (e.hasNext()) {
      if (comparingValue.equals(o,e.next())) {
        e.remove();
        return true;
      }
    }
  }
  return false;
}
