{
  boolean shouldAttemptEvict=lockedAlready || tryLock();
  if (shouldAttemptEvict) {
    try {
      eviction.execute();
    }
  finally {
      if (!lockedAlready) {
        unlock();
      }
    }
  }
}
