{
  boolean inStack=stack.containsKey(e.key);
  if (inStack)   stack.remove(e.key);
  stack.put(e.key,e);
  if (inStack) {
    queue.remove(e);
    e.transitionToLIRResident();
    switchBottomostLIRtoHIRAndPrune(evicted);
  }
 else {
    queue.remove(e);
    queue.addLast(e);
  }
}
