{
  boolean inStack=stack.containsKey(e.hashCode());
  if (inStack)   stack.remove(e.hashCode());
  stack.put(e.hashCode(),e);
  if (inStack) {
    queue.remove(e);
    e.transitionToLIRResident();
    switchBottomostLIRtoHIRAndPrune(evicted);
  }
 else {
    queue.remove(e);
    queue.addLast(e);
  }
}
