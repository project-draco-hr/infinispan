{
  Object returnValue=invokeNextInterceptor(ctx,command);
  if (returnValue == null && ctx.lookupEntry(command.getKey()) == null)   returnValue=remoteGetAndStoreInL1(ctx,command.getKey());
  return returnValue;
}
