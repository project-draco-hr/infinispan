{
  if (isRemoved() && doUndelete) {
    if (trace)     log.trace("Entry is deleted in current scope.  Need to un-delete.");
    setRemoved(false);
    setValid(true);
    return true;
  }
  return false;
}
