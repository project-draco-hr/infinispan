{
  try {
    return mapFromIterable((Iterable<Map.Entry<K,V>>)iterable);
  }
  finally {
    try {
      iterable.close();
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
}
