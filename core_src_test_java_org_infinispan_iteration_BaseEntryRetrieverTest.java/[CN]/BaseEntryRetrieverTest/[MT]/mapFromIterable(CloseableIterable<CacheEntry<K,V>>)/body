{
  try {
    return mapFromIterable((Iterable<CacheEntry<K,V>>)iterable);
  }
  finally {
    try {
      iterable.close();
    }
 catch (    Exception e) {
      throw new RuntimeException(e);
    }
  }
}
