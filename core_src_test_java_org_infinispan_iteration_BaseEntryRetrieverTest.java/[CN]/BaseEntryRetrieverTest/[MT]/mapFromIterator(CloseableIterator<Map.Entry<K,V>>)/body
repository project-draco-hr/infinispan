{
  try {
    return mapFromIterator((Iterator<Map.Entry<K,V>>)iterator);
  }
  finally {
    try {
      iterator.close();
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
}
