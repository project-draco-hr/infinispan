{
  try {
    return mapFromIterator((Iterator<Map.Entry<K,V>>)iterator);
  }
  finally {
    try {
      iterator.close();
    }
 catch (    Exception e) {
      throw new RuntimeException(e);
    }
  }
}
