{
  try {
    return mapFromIterator((Iterator<CacheEntry<K,V>>)iterator);
  }
  finally {
    try {
      iterator.close();
    }
 catch (    Exception e) {
      throw new RuntimeException(e);
    }
  }
}
