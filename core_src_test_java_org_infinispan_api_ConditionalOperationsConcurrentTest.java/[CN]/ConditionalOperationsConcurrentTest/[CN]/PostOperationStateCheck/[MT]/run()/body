{
  if (state.isAfter()) {
    cycle++;
    log.tracef("Starting cycle %d",cycle);
    if (cycle % (operations / 100) == 0) {
      print((cycle * 100 * threads / operations) + "%");
    }
    checkAfterState();
  }
 else {
    checkBeforeState();
  }
}
