{
  if (state.isAfter()) {
    cycle++;
    log.tracef("Starting cycle %d",cycle);
    if (cycle % (MOVES / 100) == 0) {
      print((cycle * 100 * THREAD_COUNT / MOVES) + "%");
    }
    checkAfterState();
  }
 else {
    checkBeforeState();
  }
}
