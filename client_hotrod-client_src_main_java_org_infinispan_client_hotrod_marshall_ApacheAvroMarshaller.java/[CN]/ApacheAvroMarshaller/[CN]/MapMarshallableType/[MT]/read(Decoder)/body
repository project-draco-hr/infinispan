{
  long size=decoder.readArrayStart();
  Map map=new HashMap((int)size);
  for (int i=0; i < size; i++)   map.put(marshaller.objectFromByteBuffer(decoder),marshaller.objectFromByteBuffer(decoder));
  return map;
}
