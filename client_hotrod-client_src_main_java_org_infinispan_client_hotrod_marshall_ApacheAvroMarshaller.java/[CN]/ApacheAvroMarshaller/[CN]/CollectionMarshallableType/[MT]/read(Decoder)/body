{
  long size=decoder.readArrayStart();
  Collection collection=createCollection((int)size);
  for (int k=0; k < size; k++)   collection.add(marshaller.objectFromByteBuffer(decoder));
  return collection;
}
