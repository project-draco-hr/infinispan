{
  long rank=ClusterIdGenerator.this.calculateRank(e.getLocalAddress(),e.getNewMembers(),e.getViewId());
  if (log.isTraceEnabled())   log.tracef("Calculated rank based on view %s and result was %d",e,rank);
}
