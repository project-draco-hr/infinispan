{
  if (cm != null)   cm.addListener(rankCalculator);
  if (rpcManager != null) {
    Transport transport=rpcManager.getTransport();
    calculateRank(rpcManager.getAddress(),transport.getMembers(),transport.getViewId());
  }
}
