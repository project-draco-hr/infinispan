{
  ConfigurationBuilder cfg=new ConfigurationBuilder();
  cfg.loaders().shared(true).addStore(DummyInMemoryCacheStoreConfigurationBuilder.class).storeName(SharedCacheStoreTest.class.getName()).purgeOnStartup(false).clustering().cacheMode(CacheMode.REPL_SYNC).build();
  createCluster(cfg,3);
}
