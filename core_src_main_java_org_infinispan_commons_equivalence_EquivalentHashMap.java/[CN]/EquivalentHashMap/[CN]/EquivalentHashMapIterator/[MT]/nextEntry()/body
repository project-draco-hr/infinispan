{
  if (modCount != expectedCount)   throw new ConcurrentModificationException();
  if (!hasNext && !hasNext())   throw new NoSuchElementException();
  current=next++;
  hasNext=false;
  Node<K,V> node=table[this.current];
  return new MapEntry<K,V>(node.key,node.value,EquivalentHashMap.this);
}
