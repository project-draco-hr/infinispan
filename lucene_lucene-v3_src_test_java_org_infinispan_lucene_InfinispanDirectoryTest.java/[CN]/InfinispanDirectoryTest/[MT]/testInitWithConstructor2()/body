{
  Directory dir=null;
  try {
    Cache cache=cacheManager.getCache();
    dir=new InfinispanDirectory(cache,"index");
    verifyDir(dir,"index");
  }
  finally {
    if (dir != null)     dir.close();
  }
}
