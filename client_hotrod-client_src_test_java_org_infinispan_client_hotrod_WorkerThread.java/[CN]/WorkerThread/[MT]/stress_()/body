{
  Random rnd=new Random();
  while (!stopWorker) {
    remoteCache.put(rnd.nextLong(),rnd.nextLong());
    try {
      Thread.sleep(50);
    }
 catch (    InterruptedException e) {
      interrupted();
      return;
    }
  }
}
