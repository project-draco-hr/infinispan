{
  this.key=key;
  this.value=value;
  try {
    trace("::putAsync::send contains: " + send.peek());
    send.put(PUT_ASYNC);
  }
 catch (  InterruptedException e) {
    throw new IllegalStateException(e);
  }
}
