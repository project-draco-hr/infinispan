{
  if (log.isTraceEnabled())   log.trace("Handling leavers: %s. There are %s prepared transactions to check",leavers,preparedTransactions.values().size());
  for (  RecoveryAwareRemoteTransaction rt : preparedTransactions.values()) {
    rt.computeOrphan(leavers);
  }
}
