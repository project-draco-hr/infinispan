{
  Class<? extends QueryInterceptor> queryType=SecurityActions.getCacheConfiguration(cache.getAdvancedCache()).indexing().index().isLocalOnly() ? LocalQueryInterceptor.class : QueryInterceptor.class;
  return getComponent(cache,queryType);
}
