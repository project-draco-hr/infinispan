{
  int h=0;
  Traverser<K,V,Object> it=new Traverser<K,V,Object>(this);
  V v;
  while ((v=it.advance()) != null) {
    h+=comparingKey.hashCode(it.nextKey) ^ comparingValue.hashCode(v);
  }
  return h;
}
