{
  if (o != null) {
    Iterator<V> it=new ValueIterator<K,V>(map);
    while (it.hasNext()) {
      if (map.comparingValue.equals(o,it.next())) {
        it.remove();
        return true;
      }
    }
  }
  return false;
}
