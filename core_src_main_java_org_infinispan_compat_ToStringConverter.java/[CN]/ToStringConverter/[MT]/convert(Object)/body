{
  if (source instanceof String)   return (String)source;
  if (source instanceof byte[])   return unmarshallString((byte[])source);
  if (source instanceof ByteArrayKey)   return unmarshallString(((ByteArrayKey)source).getData());
  return source.toString();
}
