{
  if (!servers.isEmpty()) {
    for (    Context s : servers.values()) {
      if (!s.getServer().isStarted()) {
        return false;
      }
    }
    return true;
  }
 else {
    return false;
  }
}
