{
  if (configuration.clustering().cacheMode().isDistributed()) {
    if (configuration.unsafe().unreliableReturnValues())     return (T)new NoReturnValuesDistributionResponseGenerator();
 else     return (T)new DistributionResponseGenerator();
  }
 else   return (T)new DefaultResponseGenerator();
}
