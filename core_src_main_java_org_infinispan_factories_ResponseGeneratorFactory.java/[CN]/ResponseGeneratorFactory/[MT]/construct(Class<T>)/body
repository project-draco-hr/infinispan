{
  if (configuration.getCacheMode().isDistributed()) {
    if (configuration.isUnsafeUnreliableReturnValues())     return (T)new NoReturnValuesDistributionResponseGenerator();
 else     return (T)new DistributionResponseGenerator();
  }
 else   return (T)new DefaultResponseGenerator();
}
