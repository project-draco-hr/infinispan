{
  ConfigurationBuilder cfgBuild=TestUtil.createConfigBuilder(server.getHotrodEndpoint().getInetAddress().getHostName(),server.getHotrodEndpoint().getPort());
  if (clientMarshaller != null) {
    cfgBuild.marshaller(clientMarshaller);
  }
  return new RemoteCacheManager(cfgBuild.build());
}
