{
  final int prime=31;
  int result=1;
  result=prime * result + ((deltaAwareValueKey == null) ? 0 : deltaAwareValueKey.hashCode());
  result=prime * result + ((entryKey == null) ? 0 : entryKey.hashCode());
  return result;
}
