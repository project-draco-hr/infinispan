{
  if (this == o) {
    return true;
  }
  if (!(o instanceof ApplyDeltaCommand)) {
    return false;
  }
  if (!super.equals(o)) {
    return false;
  }
  ApplyDeltaCommand that=(ApplyDeltaCommand)o;
  if (!keys.equals(that.keys)) {
    return false;
  }
  return true;
}
