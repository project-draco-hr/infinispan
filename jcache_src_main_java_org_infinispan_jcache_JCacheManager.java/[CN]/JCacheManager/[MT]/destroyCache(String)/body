{
  checkNotClosed();
  if (cacheName == null)   throw new NullPointerException("cacheName cannot be null");
  JCache<?,?> destroyedCache;
synchronized (caches) {
    destroyedCache=caches.remove(cacheName);
  }
  cm.removeCache(cacheName);
  unregisterCacheMBeans(destroyedCache);
}
