{
  Set<String> cacheNames=cm.getCacheNames();
  for (  String cacheName : cacheNames) {
    caches.put(cacheName,new JCache<Object,Object>(cm.getCache(cacheName).getAdvancedCache(),this,new MutableConfiguration<Object,Object>()));
  }
}
