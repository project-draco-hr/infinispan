{
  ConfigurationBuilder config=getDefaultConfigBuilder();
  config.security().authentication().serverName(TEST_SERVER_NAME).saslMechanism(getTestedMech()).callbackHandler(new LoginHandler(login,password,TEST_REALM)).enable();
  return config.build();
}
