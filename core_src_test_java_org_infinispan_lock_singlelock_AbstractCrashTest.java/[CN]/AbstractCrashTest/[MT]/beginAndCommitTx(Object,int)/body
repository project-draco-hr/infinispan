{
  fork(new Runnable(){
    @Override public void run(){
      try {
        tm(cacheIndex).begin();
        cache(cacheIndex).put(k,"v");
        tm(cacheIndex).commit();
      }
 catch (      Throwable e) {
        e.printStackTrace();
      }
    }
  }
,false);
  return k;
}
