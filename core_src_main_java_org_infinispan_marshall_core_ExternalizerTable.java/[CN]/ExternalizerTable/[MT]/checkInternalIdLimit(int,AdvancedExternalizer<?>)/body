{
  if (id >= Ids.MAX_ID)   throw new CacheConfigurationException(String.format("Internal %s externalizer is using an id(%d) that exceeded the limit. It needs to be smaller than %d",ext,id,Ids.MAX_ID));
  return id;
}
