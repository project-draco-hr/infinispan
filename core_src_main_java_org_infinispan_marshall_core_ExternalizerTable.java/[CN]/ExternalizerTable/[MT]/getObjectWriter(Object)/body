{
  Class<?> clazz=o.getClass();
  Writer writer=writers.get(clazz);
  if (writer == null) {
    if (Thread.currentThread().isInterrupted())     throw new IOException(log.interruptedRetrievingObjectWriter(clazz.getName()));
  }
  return writer;
}
