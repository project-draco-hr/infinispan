{
  if (!isPre) {
    if (log.isTraceEnabled())     log.tracef("Notify eviction listeners for key=%",key);
    notifier.notifyCacheEntriesEvicted(Collections.singleton(factory.create(key,value,previousMetadata)),ctx,this);
  }
}
