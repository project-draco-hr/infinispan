{
  String manifest=Descriptors.create(ManifestDescriptor.class).attribute("Dependencies","org.infinispan:" + Version.MAJOR_MINOR + " service, org.infinispan.tree:"+ Version.MAJOR_MINOR+ " services").exportAsString();
  return new StringAsset(manifest);
}
