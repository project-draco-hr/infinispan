{
  final AtomicReference<String> ref=new AtomicReference<String>();
  Thread t=new Thread(){
    public void run(){
      ref.set(cache.get(key));
    }
  }
;
  t.start();
  t.join();
  return ref.get();
}
