{
  Object configuration=cs.getConfiguration();
  assertTrue("Cache loaders doesn't provide a proper configuration type that is capable of creating the " + "loaders!",configuration instanceof CacheLoaderConfiguration);
}
