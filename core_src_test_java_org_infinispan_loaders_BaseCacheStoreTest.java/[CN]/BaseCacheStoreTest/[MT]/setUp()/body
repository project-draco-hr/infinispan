{
  try {
    cs=createCacheStore();
    CacheStoreConfiguration csc=(CacheStoreConfiguration)cs.getConfiguration();
    Assert.assertTrue("Cache store tests expect purgeSynchronously to be enabled",csc == null || csc.purgeSynchronously());
  }
 catch (  Exception e) {
    e.printStackTrace();
    throw e;
  }
}
