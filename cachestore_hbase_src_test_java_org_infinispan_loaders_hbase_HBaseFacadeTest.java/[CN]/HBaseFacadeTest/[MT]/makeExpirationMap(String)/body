{
  Map<String,byte[]> expValMap=Collections.singletonMap(EXP_VALUE_FIELD,Bytes.toBytes(value));
  Map<String,Map<String,byte[]>> expCfMap=Collections.singletonMap(COL_FAMILY_EXP,expValMap);
  return expCfMap;
}
