{
  MVCCEntry mvccEntry=createWrappedEntry(key,cacheEntry,null,false,true);
  if (cacheEntry instanceof StateChangingEntry)   mvccEntry.copyStateFlagsFrom((StateChangingEntry)cacheEntry);
  ctx.putLookedUpEntry(key,mvccEntry);
  return mvccEntry;
}
