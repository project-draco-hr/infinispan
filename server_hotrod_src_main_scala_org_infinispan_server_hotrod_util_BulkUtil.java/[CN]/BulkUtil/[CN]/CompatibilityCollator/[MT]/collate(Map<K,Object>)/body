{
  Set<K> keySet=reducedResults.keySet();
  Set<K> backingSet=new HashSet<K>(keySet.size());
  for (  K key : keySet)   backingSet.add((K)converter.unboxKey(key));
  return Immutables.immutableSetWrap(backingSet);
}
