{
  ClassLoader cl=config.classLoader();
  if (cl != null)   return cl;
 else   if (cl == null && globalCfg.classLoader() != null)   return globalCfg.classLoader();
 else   return Thread.currentThread().getContextClassLoader();
}
