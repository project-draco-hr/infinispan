{
  withCacheManager(new Callable<EmbeddedCacheManager>(){
    @Override public EmbeddedCacheManager call() throws Exception {
      Configuration c=new ConfigurationBuilder().transaction().transactionMode(NON_TRANSACTIONAL).invocationBatching().enable().build();
      return new DefaultCacheManager(c);
    }
  }
);
}
