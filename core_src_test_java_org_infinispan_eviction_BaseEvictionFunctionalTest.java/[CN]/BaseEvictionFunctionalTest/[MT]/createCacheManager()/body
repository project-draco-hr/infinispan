{
  Configuration cfg=new Configuration().fluent().eviction().strategy(getEvictionStrategy()).maxEntries(128).expiration().wakeUpInterval(100L).locking().useLockStriping(false).invocationBatching().build();
  EmbeddedCacheManager cm=TestCacheManagerFactory.createCacheManager(cfg);
  cache=cm.getCache();
  cache.addListener(new EvictionListener());
  return cm;
}
