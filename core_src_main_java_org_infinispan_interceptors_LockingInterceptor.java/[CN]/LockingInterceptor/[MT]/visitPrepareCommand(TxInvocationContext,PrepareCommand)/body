{
  try {
    return invokeNextInterceptor(ctx,command);
  }
 catch (  TimeoutException te) {
    cleanupLocks(ctx,false);
    throw te;
  }
 finally {
    if (command.isOnePhaseCommit())     cleanupLocks(ctx,true);
  }
}
