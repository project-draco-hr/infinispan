{
  try {
    for (    Object key : dataContainer.keySet())     entryFactory.wrapEntryForWriting(ctx,key,false,false,false,false);
    return invokeNextInterceptor(ctx,command);
  }
  finally {
    doAfterCall(ctx);
  }
}
