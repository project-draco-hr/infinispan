{
  Thread.currentThread().setName(String.format("ReducerThread-%d",Thread.currentThread().getId()));
  int sum=0;
  while (iter.hasNext()) {
    Integer i=iter.next();
    sum+=i;
  }
  return sum;
}
