{
  if (!configuration.isEnableVersioning())   return null;
switch (configuration.getVersioningScheme()) {
case SIMPLE:
{
      if (configuration.getCacheMode().isClustered())       return (T)new SimpleClusteredVersionGenerator();
 else       return null;
    }
default :
  return null;
}
}
