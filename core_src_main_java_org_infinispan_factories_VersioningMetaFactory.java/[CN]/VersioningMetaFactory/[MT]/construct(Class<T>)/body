{
  if (!configuration.versioning().enabled())   return null;
switch (configuration.versioning().scheme()) {
case SIMPLE:
{
      if (configuration.clustering().cacheMode().isClustered())       return (T)new SimpleClusteredVersionGenerator();
 else       return null;
    }
default :
  return null;
}
}
