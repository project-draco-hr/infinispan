{
  String prevName=null;
  for (  User u : list) {
    assertNotNull(u.getName());
    if (prevName != null) {
      assertTrue(u.getName().compareTo(prevName) >= 0);
    }
    prevName=u.getName();
  }
}
