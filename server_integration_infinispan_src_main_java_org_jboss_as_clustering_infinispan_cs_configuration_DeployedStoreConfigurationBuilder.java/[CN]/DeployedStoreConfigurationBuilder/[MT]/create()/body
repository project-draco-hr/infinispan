{
  return new DeployedStoreConfiguration(purgeOnStartup,fetchPersistentState,ignoreModifications,async.create(),singletonStore.create(),preload,shared,properties,persistenceConfigurationBuilder,customStoreClassName);
}
