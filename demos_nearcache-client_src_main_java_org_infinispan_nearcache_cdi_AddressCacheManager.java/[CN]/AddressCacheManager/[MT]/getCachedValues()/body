{
  List<String> values=new ArrayList<String>();
  for (  Map.Entry<CacheKey,Address> entry : cache.entrySet())   values.add(String.format("%s -> %s",entry.getKey(),entry.getValue()));
  return values.toArray(new String[values.size()]);
}
