{
  cacheFactory1=new CompatibilityCacheFactory<String,Object>(CacheMode.DIST_SYNC,numOwners).setup();
  cacheFactory2=new CompatibilityCacheFactory<String,Object>(CacheMode.DIST_SYNC,numOwners).setup(cacheFactory1.getHotRodPort(),100);
}
