{
  writeLock.lock();
  try {
    logDebug("Ownership lost, closing index manager");
    nextOwner=newOwner;
    flushAndReleaseResources();
    hasOwnership=false;
  }
  finally {
    writeLock.unlock();
  }
}
