{
  if (tcc.getTreeContext() == null)   return flags;
 else {
    EnumSet<Flag> ctxFlags=tcc.getTreeContext().getFlags();
    if (!ctxFlags.isEmpty()) {
      EnumSet<Flag> copy=ctxFlags.clone();
      copy.addAll(flags);
      return copy;
    }
 else {
      return flags;
    }
  }
}
