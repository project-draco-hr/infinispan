{
  long start=timeService.time();
  Map<Address,Response> ret=actual.invokeRemotely(recipients,rpcCommand,mode,timeout,usePriorityQueue,responseFilter);
  updateStats(rpcCommand,mode.isSynchronous(),timeService.timeDuration(start,NANOSECONDS),recipients);
  return ret;
}
