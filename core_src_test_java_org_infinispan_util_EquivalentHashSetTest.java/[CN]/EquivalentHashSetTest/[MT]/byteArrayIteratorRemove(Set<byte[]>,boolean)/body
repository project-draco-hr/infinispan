{
  set1.add(new byte[]{1,2,3});
  Iterator<byte[]> it1=set1.iterator();
  byte[] entry1=it1.next();
  if (expectRemove) {
    it1.remove();
    assertTrue(set1.isEmpty());
  }
 else   assertFalse(set1.isEmpty());
}
