{
  byte[] entry={1,2,3};
  set.add(entry);
  byte[] remove={1,2,3};
  if (expectRemove)   assertTrue(String.format("Expected entry=%s to be removed",str(remove)),set.remove(remove));
 else   assertFalse(set.remove(remove));
}
