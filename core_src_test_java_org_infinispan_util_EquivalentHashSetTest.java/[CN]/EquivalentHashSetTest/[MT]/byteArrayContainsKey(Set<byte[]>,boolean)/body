{
  byte[] entry={1,2,3};
  set.add(entry);
  byte[] lookup={1,2,3};
  if (expectFound)   assertTrue(String.format("Expected entry=%s to be in collection",str(lookup)),set.contains(lookup));
 else   assertFalse(set.contains(lookup));
}
