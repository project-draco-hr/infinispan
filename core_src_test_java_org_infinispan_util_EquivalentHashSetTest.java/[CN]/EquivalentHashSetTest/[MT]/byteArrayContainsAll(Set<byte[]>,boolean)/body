{
  byte[] entry={1,2,3};
  set.add(entry);
  Set<byte[]> data=new HashSet<byte[]>();
  data.add(new byte[]{1,2,3});
  if (expectFound)   assertTrue(set.containsAll(data));
 else   assertFalse(set.containsAll(data));
}
