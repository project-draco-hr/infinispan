{
  byte[] entry={1,2,3};
  set.add(entry);
  byte[] entry2={1,2,3};
  if (expectFound)   assertFalse(String.format("Expected putting %s again to return true",str(entry)),set.add(entry2));
 else   assertTrue(set.add(entry2));
}
