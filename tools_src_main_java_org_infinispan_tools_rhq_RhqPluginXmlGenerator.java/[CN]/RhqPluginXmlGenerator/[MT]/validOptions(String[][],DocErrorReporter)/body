{
  for (  String[] option : options) {
    if (option[0].equals("-docletpath") || option[0].equals("-classpath"))     cp.append(option[1]).append(File.pathSeparator);
  }
  return Standard.validOptions(options,reporter);
}
