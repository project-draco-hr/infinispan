{
  while (Thread.currentThread().isInterrupted()) {
    try {
      joinInProgress.await();
      return;
    }
 catch (    InterruptedException ie) {
      if (trace)       log.trace("Interrupted while waiting for the joinInProgress gate");
      Thread.currentThread().interrupt();
    }
  }
}
