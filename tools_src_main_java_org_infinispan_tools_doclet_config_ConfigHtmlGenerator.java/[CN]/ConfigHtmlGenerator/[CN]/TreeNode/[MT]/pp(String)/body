{
  StringBuffer result=new StringBuffer(prefix + "&lt;<a href=\"" + "#ce_"+ parent.name+ "_"+ name+ "\">"+ name+ "</a>&gt;"+ "\n");
  String newPrefix=prefix + "  ";
  for (  TreeNode child : children)   result.append(child.pp(newPrefix));
  return result.toString();
}
