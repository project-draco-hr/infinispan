{
  if (System.getSecurityManager() != null) {
    return AccessController.doPrivileged(new PrivilegedAction<RpcManager>(){
      @Override public RpcManager run(){
        return cache.getRpcManager();
      }
    }
);
  }
 else {
    return cache.getRpcManager();
  }
}
