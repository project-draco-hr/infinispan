{
  if (System.getSecurityManager() != null) {
    return AccessController.doPrivileged(new PrivilegedAction<DistributionManager>(){
      @Override public DistributionManager run(){
        return cache.getDistributionManager();
      }
    }
);
  }
 else {
    return cache.getDistributionManager();
  }
}
