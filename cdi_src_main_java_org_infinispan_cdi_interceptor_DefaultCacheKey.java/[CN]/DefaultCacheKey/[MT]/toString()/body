{
  final StringBuilder builder=new StringBuilder().append("CacheKey{").append("parameters=").append(parameters == null ? null : deepToString(parameters)).append(", hashCode=").append(hashCode).append('}');
  return builder.toString();
}
