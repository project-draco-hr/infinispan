{
  for (int index=0; index < caches().size(); ++index) {
    if (mode.isSynchronous() && syncCommit) {
      assertEquals(index,key,value);
    }
 else {
      assertEventuallyEquals(index,key,value);
    }
  }
}
