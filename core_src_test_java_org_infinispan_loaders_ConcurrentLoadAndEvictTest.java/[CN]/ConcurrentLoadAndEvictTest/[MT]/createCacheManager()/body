{
  sdi=new SlowDownInterceptor();
  Configuration config=new Configuration().fluent().loaders().addCacheLoader(new DummyInMemoryCacheStore.Cfg()).customInterceptors().add(sdi).after(InvocationContextInterceptor.class).transaction().transactionalCache(false).build();
  return TestCacheManagerFactory.createCacheManager(config);
}
