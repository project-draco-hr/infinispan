{
  sdi=new SlowDownInterceptor();
  Configuration config=new Configuration().fluent().loaders().addCacheLoader(new DummyInMemoryCacheStore.Cfg()).customInterceptors().add(sdi).after(CacheLoaderInterceptor.class).build();
  return TestCacheManagerFactory.createCacheManager(config);
}
