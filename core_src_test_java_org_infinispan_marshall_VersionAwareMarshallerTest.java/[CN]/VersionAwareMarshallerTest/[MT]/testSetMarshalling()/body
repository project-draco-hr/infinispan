{
  Set s1=new HashSet();
  Set s2=new TreeSet();
  for (int i=0; i < 10; i++) {
    Integer integ=new Integer(1000 * i);
    s1.add(integ);
    s2.add(integ);
  }
  marshallAndAssertEquality(s1);
  marshallAndAssertEquality(s2);
}
