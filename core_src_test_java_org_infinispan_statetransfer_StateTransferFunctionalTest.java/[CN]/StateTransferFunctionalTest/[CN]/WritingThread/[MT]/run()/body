{
  int c=0;
  while (!stop) {
    try {
      if (tx)       tm.begin();
      cache.put("test" + c,c++);
      if (tx)       tm.commit();
    }
 catch (    Exception e) {
      c--;
      stopThread();
    }
  }
  result=c;
}
