{
  if (requiresEquivalent(keyEq,valueEq))   return new EquivalentConcurrentHashMapV8<>(initCapacity,concurrencyLevel,keyEq,valueEq);
 else   return makeConcurrentMap(initCapacity,concurrencyLevel);
}
