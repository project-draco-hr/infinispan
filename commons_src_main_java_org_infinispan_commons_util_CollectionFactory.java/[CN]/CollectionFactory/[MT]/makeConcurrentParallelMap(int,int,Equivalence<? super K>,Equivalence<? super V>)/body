{
  if (requiresEquivalent(keyEq,valueEq))   return new ConcurrentParallelHashMapV8<>(initCapacity,concurrencyLevel,keyEq,valueEq);
 else   return makeConcurrentParallelMap(initCapacity,concurrencyLevel);
}
