{
  if (requiresEquivalent(keyEq,valueEq))   return new EquivalentLinkedHashMap<>(initialCapacity,loadFactor,iterationOrder,keyEq,valueEq);
 else   return new LinkedHashMap<>(initialCapacity,loadFactor,iterationOrder.toJdkAccessOrder());
}
