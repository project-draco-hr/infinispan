{
  if (requiresEquivalent(keyEq,valueEq))   return new EquivalentConcurrentHashMapV8<K,V>(initCapacity,loadFactor,concurrencyLevel,keyEq,valueEq);
 else   return makeConcurrentMap(initCapacity,loadFactor,concurrencyLevel);
}
