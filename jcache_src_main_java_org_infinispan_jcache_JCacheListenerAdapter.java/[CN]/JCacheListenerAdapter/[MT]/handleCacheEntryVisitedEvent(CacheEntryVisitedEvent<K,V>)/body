{
  if (!e.isPre()) {
    JCacheNotifier<K,V> notifier=cache.getNotifier();
    CacheEntryEvent<? extends K,? extends V> event=new RICacheEntryEvent<K,V>(cache,e.getKey(),e.getValue());
    notifier.notifyEntryRead(event);
  }
}
