{
  InternalCacheEntry e=peek(k);
  if (e != null) {
    if (e.isExpired()) {
      if (mortalEntries.remove(k) != null) {
        numEntries.getAndDecrement();
      }
      e=null;
    }
 else {
      e.touch();
    }
  }
  return e;
}
