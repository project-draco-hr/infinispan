{
  try {
    int result=is.read();
    if (result < 0) {
      throw new TransportException("Unexpected end of stream " + result);
    }
    return result;
  }
 catch (  IOException e) {
    throw new TransportException(e);
  }
}
