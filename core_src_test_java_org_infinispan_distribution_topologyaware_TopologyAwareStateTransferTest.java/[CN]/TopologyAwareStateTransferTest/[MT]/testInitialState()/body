{
  cache(0).put(a0,"v0");
  cache(0).put(a1,"v0");
  cache(0).put(a2,"v0");
  cache(0).put(a3,"v0");
  cache(0).put(a4,"v0");
  assertExistence(a0);
  assertExistence(a1);
  assertExistence(a2);
  assertExistence(a3);
  assertExistence(a4);
}
