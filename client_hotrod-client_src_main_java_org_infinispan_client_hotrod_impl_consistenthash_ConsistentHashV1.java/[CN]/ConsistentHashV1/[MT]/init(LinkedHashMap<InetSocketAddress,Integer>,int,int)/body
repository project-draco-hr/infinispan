{
  for (  InetSocketAddress addr : servers2HashCode.keySet()) {
    positions.put(servers2HashCode.get(addr),addr);
  }
  if (log.isTraceEnabled())   log.tracef("Positions are: %s",positions);
  this.hashSpace=hashSpace;
}
