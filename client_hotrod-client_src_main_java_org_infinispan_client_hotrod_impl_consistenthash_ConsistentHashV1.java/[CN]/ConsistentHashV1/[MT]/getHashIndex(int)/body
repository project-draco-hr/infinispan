{
  int result=Arrays.binarySearch(hashes,normalisedHashForKey);
  if (result >= 0) {
    return result;
  }
 else {
    if (result == (-hashes.length - 1)) {
      return 0;
    }
 else {
      return -result - 1;
    }
  }
}
