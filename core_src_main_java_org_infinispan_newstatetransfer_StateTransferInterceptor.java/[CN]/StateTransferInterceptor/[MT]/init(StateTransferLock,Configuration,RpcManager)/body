{
  this.stateTransferLock=stateTransferLock;
  this.rpcManager=rpcManager;
  this.rpcTimeout=configuration.clustering().cacheMode().isSynchronous() ? configuration.clustering().sync().replTimeout() : 0;
}
