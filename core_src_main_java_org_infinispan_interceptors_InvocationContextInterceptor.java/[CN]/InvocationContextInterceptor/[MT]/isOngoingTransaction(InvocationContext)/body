{
  if (!ctx.isInTxScope())   return false;
  if (ctx.isOriginLocal())   return txTable.containsLocalTx(tm.getTransaction());
 else   return txTable.containRemoteTx(((TxInvocationContext)ctx).getGlobalTransaction());
}
