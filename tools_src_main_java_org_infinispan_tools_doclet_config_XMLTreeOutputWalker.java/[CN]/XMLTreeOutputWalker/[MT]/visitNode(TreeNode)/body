{
  String ident="";
  for (int i=0; i <= treeNode.getDepth(); i++)   ident+=IDENT;
  sb.append(ident + "&lt;<a href=\"" + "#ce_"+ treeNode.getParent().getName()+ "_"+ treeNode.getName()+ "\">"+ treeNode.getName()+ "</a>&gt;"+ "\n");
}
