{
  isShutdown.set(true);
  if (takeExecutorOwnership) {
    if (interrupt)     localExecutorService.shutdownNow();
 else     localExecutorService.shutdown();
  }
  return InfinispanCollections.emptyList();
}
