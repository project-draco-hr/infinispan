{
  if (runnable == null)   throw new NullPointerException();
  DistributedExecuteCommand<T> executeCommand=factory.buildDistributedExecuteCommand(new RunnableAdapter<T>(runnable,value),rpc.getAddress(),null);
  return new DistributedRunnableFuture<T>(executeCommand);
}
