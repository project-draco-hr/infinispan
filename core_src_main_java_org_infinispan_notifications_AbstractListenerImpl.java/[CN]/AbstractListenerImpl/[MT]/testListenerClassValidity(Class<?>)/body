{
  Listener l=ReflectionUtil.getAnnotation(listenerClass,Listener.class);
  if (l == null)   throw new IncorrectListenerException("Cache listener class MUST be annotated with org.infinispan.notifications.annotation.Listener");
  if (!Modifier.isPublic(listenerClass.getModifiers()))   throw new IncorrectListenerException("Cache listener class MUST be public!");
  return l.sync();
}
