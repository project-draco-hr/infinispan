{
  try {
    cleanupExecutor.submit(new Callable<Void>(){
      public Void call(){
        cleanupLeaverTransactions(e.getNewMembers());
        return null;
      }
    }
);
  }
 catch (  RejectedExecutionException x) {
    if (!cleanupExecutor.isShutdown())     throw x;
  }
}
