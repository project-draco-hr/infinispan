{
  short status=sendKeyOperation(key,transport,REMOVE_REQUEST,REMOVE_RESPONSE);
  byte[] result=returnPossiblePrevValue(transport);
  if (status == KEY_DOES_NOT_EXIST_STATUS)   return null;
  return result;
}
