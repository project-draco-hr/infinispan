{
  final Configuration defaultInjectedConfiguration=getBeanReference(Configuration.class);
  getDefaultCacheManager().defineConfiguration(cacheName,defaultInjectedConfiguration);
  return getDefaultJCacheManager().getOrCreateCache(cacheName,getDefaultCacheManager().<K,V>getCache(cacheName).getAdvancedCache());
}
