{
  if (wrapKeys) {
    for (    Object key : command.getKeys()) {
      if (key instanceof MarshalledValue)       ((MarshalledValue)key).setEqualityPreferenceForInstance(isCompareInstance);
    }
  }
}
