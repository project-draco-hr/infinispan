{
  boolean isRemote=!ctx.isOriginLocal();
  if (isRemote)   forceComparison(false,command);
  try {
    return invokeNextInterceptor(ctx,command);
  }
  finally {
    if (isRemote)     forceComparison(true,command);
  }
}
