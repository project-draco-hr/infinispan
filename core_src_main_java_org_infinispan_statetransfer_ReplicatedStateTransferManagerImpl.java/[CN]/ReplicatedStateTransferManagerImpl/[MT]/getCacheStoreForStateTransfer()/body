{
  if (cacheLoaderManager == null || !cacheLoaderManager.isEnabled() || cacheLoaderManager.isShared() || !cacheLoaderManager.isFetchPersistentState())   return null;
  return cacheLoaderManager.getCacheStore();
}
