{
  if (trace) {
    log.trace("Clearing store");
  }
  boolean success=acquireGlobalLock(true);
  try {
    clearLockSafe();
  }
  finally {
    if (success) {
      releaseGlobalLock(true);
    }
  }
}
