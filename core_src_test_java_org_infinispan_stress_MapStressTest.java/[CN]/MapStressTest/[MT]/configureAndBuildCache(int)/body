{
  Configuration config=new Configuration().fluent().eviction().maxEntries(capacity).strategy(EvictionStrategy.LRU).wakeUpInterval(5000L).expiration().maxIdle(120000L).build();
  DefaultCacheManager cm=new DefaultCacheManager(GlobalConfiguration.getNonClusteredDefault(),config);
  cm.start();
  return cm.getCache();
}
