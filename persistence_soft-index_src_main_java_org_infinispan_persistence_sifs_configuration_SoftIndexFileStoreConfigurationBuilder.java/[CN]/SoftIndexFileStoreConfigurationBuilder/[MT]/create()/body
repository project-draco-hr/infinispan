{
  return new SoftIndexFileStoreConfiguration(purgeOnStartup,fetchPersistentState,ignoreModifications,async.create(),singletonStore.create(),preload,shared,properties,dataLocation,indexLocation,indexSegments,maxFileSize,minNodeSize < 0 ? maxNodeSize / 3 : minNodeSize,maxNodeSize,indexQueueLength,syncWrites,openFilesLimit,compactionThreshold);
}
