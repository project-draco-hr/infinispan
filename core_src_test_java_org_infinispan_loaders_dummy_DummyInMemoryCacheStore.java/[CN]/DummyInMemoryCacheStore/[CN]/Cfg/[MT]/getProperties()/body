{
  Properties p=super.getProperties();
  p.setProperty("debug",Boolean.toString(debug));
  if (storeName != null)   p.setProperty("storeName",storeName);
  if (failKey != null)   p.setProperty("storeName",failKey.toString());
  return p;
}
