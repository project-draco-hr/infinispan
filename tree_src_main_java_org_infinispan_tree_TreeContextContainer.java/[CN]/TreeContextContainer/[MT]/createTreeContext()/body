{
  TreeContext existing=tcTL.get();
  if (existing == null) {
    TreeContext treeContext=new TreeContext();
    tcTL.set(treeContext);
    return treeContext;
  }
 else {
    return existing;
  }
}
