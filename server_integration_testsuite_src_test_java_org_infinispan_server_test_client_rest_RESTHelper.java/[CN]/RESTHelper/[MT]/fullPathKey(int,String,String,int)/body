{
  StringBuffer sb=new StringBuffer("http://" + servers.get(server).getHostname() + ":"+ (port + offset)+ servers.get(server).getRestServerPath()+ "/"+ cache);
  if (key == null) {
    return sb.toString();
  }
 else {
    return sb.append("/").append(key).toString();
  }
}
