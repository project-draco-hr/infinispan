{
  if (fetchPersistentState) {
    String message="fetchPersistentState cannot be set for remote cache store. This is because " + "persistent state cannot be generated by RemoteCacheStore, and state generation is proprietary. I.e. this " + "store would not be able to integrate state generated by other store implementations.";
    log.error(message);
    throw new IllegalStateException(message);
  }
 else {
    super.setFetchPersistentState(fetchPersistentState);
  }
}
