{
synchronized (cacheManagers) {
    for (    Map<URI,JCacheManager> map : cacheManagers.values())     close(map);
    cacheManagers.clear();
    if (log.isTraceEnabled())     log.tracef("All cache managers have been removed");
  }
}
