{
  boolean backendConfiguredAsync=!BackendFactory.isConfiguredAsSync(properties);
  return ConfigurationParseHelper.getBooleanValue(properties,InfinispanIntegration.WRITE_METADATA_ASYNC,backendConfiguredAsync);
}
