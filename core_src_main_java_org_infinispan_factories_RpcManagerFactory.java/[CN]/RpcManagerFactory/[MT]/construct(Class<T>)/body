{
  if (!configuration.clustering().cacheMode().isClustered())   return null;
  if (globalConfiguration.transport().transport() == null)   throw new ConfigurationException("Transport should be configured in order to use clustered caches");
  return componentType.cast(new RpcManagerImpl());
}
