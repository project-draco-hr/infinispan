{
  if (!configuration.getCacheMode().isClustered())   return null;
  if (globalConfiguration.getTransportClass() == null)   throw new ConfigurationException("Transport should be configured in order to use clustered caches");
  return super.construct(componentType);
}
