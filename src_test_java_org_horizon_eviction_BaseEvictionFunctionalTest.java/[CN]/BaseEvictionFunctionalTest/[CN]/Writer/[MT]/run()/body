{
  try {
    startLatch.await();
  }
 catch (  InterruptedException e) {
  }
  while (running) {
    try {
      sleep(r.nextInt(5) * 10);
    }
 catch (    InterruptedException e) {
    }
    cache.put("key" + r.nextInt(),"value");
  }
}
