{
  async.validate();
  singletonStore.validate();
  if (!getLoadersBuilder().shared() && !fetchPersistentState && !purgeOnStartup&& getBuilder().clustering().cacheMode().isClustered()&& cacheLoader instanceof CacheStore)   log.staleEntriesWithoutFetchPersistentStateOrPurgeOnStartup();
}
