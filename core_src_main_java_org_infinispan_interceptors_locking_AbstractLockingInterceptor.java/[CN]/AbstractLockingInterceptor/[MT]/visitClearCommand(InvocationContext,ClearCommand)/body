{
  try {
    lockForClear(ctx);
    return invokeNextInterceptor(ctx,command);
  }
 catch (  Throwable te) {
    return cleanLocksAndRethrow(ctx,te);
  }
 finally {
    releaseLocksIfNeeded(ctx);
  }
}
