{
  InternalCacheEntry e=immortalEntries.get(k);
  if (e == null) {
    e=mortalEntries.get(k);
    if (e != null) {
      if (e.isExpired()) {
        mortalEntries.remove(k);
        e=null;
      }
 else {
        e.touch();
      }
    }
  }
  return e;
}
