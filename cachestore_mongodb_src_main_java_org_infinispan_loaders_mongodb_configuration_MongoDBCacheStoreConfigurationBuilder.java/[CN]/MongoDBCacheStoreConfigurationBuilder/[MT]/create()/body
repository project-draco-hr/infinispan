{
  return new MongoDBCacheStoreConfiguration(host,port,timeout,username,password,database,collection,acknowledgment,purgeOnStartup,purgeSynchronously,purgerThreads,fetchPersistentState,ignoreModifications,TypedProperties.toTypedProperties(properties),async.create(),singletonStore.create());
}
