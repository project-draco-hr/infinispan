{
  int numOwners=configuration.clustering().hash().numOwners();
  return isStateTransferInProgress() && !chOld.isKeyLocalToAddress(getAddress(),key,numOwners) && chNew.isKeyLocalToAddress(getAddress(),key,numOwners);
}
