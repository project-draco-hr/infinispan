{
  if (addIfAbsent) {
    for (    CacheEntryListenerRegistration<? super K,? super V> reg : listeners) {
      if (reg.getCacheEntryListener().equals(listener))       return true;
    }
  }
  return false;
}
