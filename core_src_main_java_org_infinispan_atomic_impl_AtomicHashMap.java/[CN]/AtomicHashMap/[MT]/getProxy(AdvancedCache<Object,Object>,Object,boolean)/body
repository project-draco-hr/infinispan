{
  if (proxy == null) {
synchronized (this) {
      if (proxy == null)       if (fineGrained) {
        proxy=new FineGrainedAtomicHashMapProxy<K,V>(cache,mapKey);
      }
 else {
        proxy=new AtomicHashMapProxy<K,V>(cache,mapKey);
      }
    }
  }
  return proxy;
}
