{
  if (configuration.getCacheMode().isClustered()) {
    notifier.removeListener(cleanupService);
    cm.removeListener(cleanupService);
    cleanupService.stop();
    cm.removeListener(this);
    currentViewId=CACHE_STOPPED_VIEW_ID;
  }
  shutDownGracefully();
}
