{
  if (System.getSecurityManager() != null) {
    return AccessController.doPrivileged(new PrivilegedAction<ComponentRegistry>(){
      @Override public ComponentRegistry run(){
        return cache.getComponentRegistry();
      }
    }
);
  }
 else {
    return cache.getComponentRegistry();
  }
}
