{
  if (enabled) {
    waitForModsLatch();
    if (enabled) {
      GlobalTransaction gtx;
      if (!uncommittedPrepares.containsKey(gtx=commit.getGlobalTransaction()))       uncommittedPrepares.put(gtx,cf.buildPrepareCommand(gtx,context.getModifications(),false));
    }
  }
}
