{
  txLock.readLock().unlock();
  if (loggingEnabled) {
    PrepareCommand pc=uncommittedPrepares.remove(command.getGlobalTransaction());
    if (pc == null)     logModifications(context.getModifications());
 else     logModificationsInTransaction(pc);
  }
}
