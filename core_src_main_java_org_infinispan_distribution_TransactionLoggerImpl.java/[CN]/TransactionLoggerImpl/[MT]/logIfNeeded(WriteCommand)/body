{
  loggingLock.readLock().lock();
  try {
    if (enabled) {
      commandQueue.add(command);
      return true;
    }
 else {
      return false;
    }
  }
  finally {
    loggingLock.readLock().unlock();
  }
}
