{
  if (partitionHandlingManager.getAvailabilityMode() != AvailabilityMode.AVAILABLE && (command.getFlags() == null || !command.getFlags().contains(Flag.CACHE_MODE_LOCAL))) {
    throw getLog().partitionUnavailable();
  }
  return super.visitEntryRetrievalCommand(ctx,command);
}
