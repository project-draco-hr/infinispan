{
  if (results == null) {
    results=SecurityActions.doPrivileged(new PrivilegedAction<List>(){
      @Override public List run(){
        return listInternal();
      }
    }
);
  }
  return results;
}
