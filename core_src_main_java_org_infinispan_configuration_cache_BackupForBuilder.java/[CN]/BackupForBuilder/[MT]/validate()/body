{
  if (remoteCache == null && remoteSite == null)   return;
  if (remoteSite == null || remoteCache == null) {
    throw new ConfigurationException("Both 'remoteCache' and 'remoteSite' must be specified for a backup'!");
  }
}
