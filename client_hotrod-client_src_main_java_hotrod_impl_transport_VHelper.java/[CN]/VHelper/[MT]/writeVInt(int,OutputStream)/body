{
  boolean hasMore;
  do {
    int currentByte=toWrite & 0x0000007F;
    toWrite=toWrite >> 7;
    hasMore=toWrite > 0;
    if (hasMore) {
      currentByte|=128;
    }
    writeByte(os,currentByte);
  }
 while (hasMore);
}
