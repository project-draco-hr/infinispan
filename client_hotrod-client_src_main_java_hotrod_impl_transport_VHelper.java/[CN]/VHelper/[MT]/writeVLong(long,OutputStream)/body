{
  boolean hasMore;
  do {
    long currentByte=toWrite & 0x000000000000007F;
    toWrite=toWrite >> 7;
    hasMore=toWrite > 0;
    if (hasMore) {
      currentByte|=128;
    }
    writeByte(os,(int)currentByte);
  }
 while (hasMore);
}
