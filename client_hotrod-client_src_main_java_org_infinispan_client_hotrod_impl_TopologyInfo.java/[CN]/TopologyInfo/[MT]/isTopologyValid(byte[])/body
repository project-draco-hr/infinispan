{
  Integer id=topologyIds.get(cacheName).get();
  Boolean valid=id != HotRodConstants.SWITCH_CLUSTER_TOPOLOGY;
  if (log.isTraceEnabled())   log.tracef("Is topology id (%s) valid? %b",id,valid);
  return valid;
}
