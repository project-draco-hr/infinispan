{
  int i=mods.size() - 1;
  for (; i >= 0; i--)   if (mods.get(i).getType() == Modification.Type.CLEAR)   break;
  if (i >= 0) {
    clear();
    mods=mods.subList(i + 1,mods.size());
  }
  if (!mods.isEmpty())   put(new ModificationsList(mods),mods.size());
}
