{
  try {
    if (!lock.tryLock(asyncStoreConfig.getMapLockTimeout(),TimeUnit.MILLISECONDS))     throw new CacheException("Unable to acquire lock on update map");
  }
 catch (  InterruptedException ie) {
    Thread.currentThread().interrupt();
  }
}
