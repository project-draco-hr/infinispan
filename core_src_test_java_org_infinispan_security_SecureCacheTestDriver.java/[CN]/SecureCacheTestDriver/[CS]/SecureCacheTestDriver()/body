{
  interceptor=new CommandInterceptor(){
  }
;
  keyFilter=new KeyFilter<String>(){
    @Override public boolean accept(    String key){
      return true;
    }
  }
;
  keyValueFilter=new KeyValueFilter<String,String>(){
    @Override public boolean accept(    String key,    String value,    Metadata metadata){
      return true;
    }
  }
;
  converter=new Converter<String,String,String>(){
    @Override public String convert(    String key,    String value,    Metadata metadata){
      return value;
    }
  }
;
  listener=new NullListener();
  metadata=new Metadata(){
    @Override public long lifespan(){
      return -1;
    }
    @Override public long maxIdle(){
      return -1;
    }
    @Override public EntryVersion version(){
      return null;
    }
    @Override public Builder builder(){
      return null;
    }
  }
;
}
