{
  return newKeyAffinityService(cache,new ExecutorFactory(){
    @Override public ExecutorService getExecutor(    Properties p){
      return Executors.newSingleThreadExecutor();
    }
  }
,new RndKeyProvider(),keyBufferSize);
}
