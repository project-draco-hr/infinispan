{
  ConfigurationBuilder builder=new ConfigurationBuilder();
  builder.clustering().hash().consistentHash(new DefaultConsistentHash()).numVirtualNodes(numVirtualNodes);
  return (DefaultConsistentHash)ConsistentHashHelper.createConsistentHash(builder.build(),false,servers);
}
