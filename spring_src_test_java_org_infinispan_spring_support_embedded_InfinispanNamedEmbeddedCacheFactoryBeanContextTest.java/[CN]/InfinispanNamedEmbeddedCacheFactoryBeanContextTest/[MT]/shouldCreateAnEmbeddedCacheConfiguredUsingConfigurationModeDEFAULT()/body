{
  final Cache<Object,Object> infinispanNamedEmbeddedCacheConfiguredUsingConfigurationModeDefault=this.applicationContext.getBean(Infinispan_NAMED_EMBEDDED_CACHE_CONFIGURED_USING_MODE_DEFAULT_BEAN_NAME,Cache.class);
  assertNotNull("Spring application context should contain a named Infinispan cache having bean name = \"" + Infinispan_NAMED_EMBEDDED_CACHE_CONFIGURED_USING_MODE_DEFAULT_BEAN_NAME + "\" that has been configured using configuration mode DEFAULT. However, it doesn't.",infinispanNamedEmbeddedCacheConfiguredUsingConfigurationModeDefault);
}
