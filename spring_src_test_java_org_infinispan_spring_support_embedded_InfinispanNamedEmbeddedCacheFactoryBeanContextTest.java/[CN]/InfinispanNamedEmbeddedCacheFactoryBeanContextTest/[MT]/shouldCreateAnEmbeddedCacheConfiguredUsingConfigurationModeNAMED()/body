{
  final Cache<Object,Object> infinispanNamedEmbeddedCacheConfiguredUsingConfigurationModeNamed=this.applicationContext.getBean(Infinispan_NAMED_EMBEDDED_CACHE_CONFIGURED_USING_MODE_NAMED_BEAN_NAME,Cache.class);
  assertNotNull("Spring application context should contain a named Infinispan cache having bean name = \"" + Infinispan_NAMED_EMBEDDED_CACHE_CONFIGURED_USING_MODE_NAMED_BEAN_NAME + "\" that has been configured using configuration mode NAMED. However, it doesn't.",infinispanNamedEmbeddedCacheConfiguredUsingConfigurationModeNamed);
}
