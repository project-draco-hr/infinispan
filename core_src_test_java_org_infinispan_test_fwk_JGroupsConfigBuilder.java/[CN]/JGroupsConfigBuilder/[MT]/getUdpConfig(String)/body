{
  loadUdp();
  String config=udpConfig;
  if (config.contains("TEST_PING")) {
    if (fullTestName != null)     config=getTestPingDiscovery(fullTestName,config);
  }
  return replaceMCastAddressAndPort(config);
}
