{
  EntryBinding storedEntryKeyBinding=new SerialBinding(classCatalog,Object.class);
  EntryBinding storedEntryValueBinding=new SerialBinding(classCatalog,InternalCacheEntry.class);
  try {
    return new StoredMap<Object,InternalCacheEntry>(database,storedEntryKeyBinding,storedEntryValueBinding,true);
  }
 catch (  Exception caught) {
    caught=ExceptionUnwrapper.unwrap(caught);
    throw new DatabaseException("error opening stored map",caught);
  }
}
