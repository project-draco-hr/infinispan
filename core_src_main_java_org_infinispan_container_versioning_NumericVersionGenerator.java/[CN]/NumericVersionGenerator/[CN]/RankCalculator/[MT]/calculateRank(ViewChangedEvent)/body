{
  long rank=NumericVersionGenerator.this.calculateRank(e.getLocalAddress(),e.getNewMembers(),e.getViewId());
  if (trace)   log.tracef("Calculated rank based on view %s and result was %d",e,rank);
}
