{
  clearTempDir();
  fcs=new FileCacheStore();
  FileCacheStoreConfig cfg=new FileCacheStoreConfig().fetchPersistentState(true).fsyncMode(getFsyncMode()).location(tmpDirectory).purgeSynchronously(true);
  fcs.init(cfg,getCache(),getMarshaller());
  fcs.start();
  return fcs;
}
