{
  this.cacheManagers=cacheManagers;
  for (  EmbeddedCacheManager cacheManager : cacheManagers)   jcacheManagers.put(cacheManager,toJCacheManager(cacheManager));
  this.defaultCacheManager=cacheManagers.select(new AnnotationLiteral<Default>(){
  }
).get();
  this.defaultJCacheManager=jcacheManagers.get(defaultCacheManager);
}
