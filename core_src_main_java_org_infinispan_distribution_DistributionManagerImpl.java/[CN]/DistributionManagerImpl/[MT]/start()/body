{
  if (trace)   log.tracef("starting distribution manager on %s",getAddress());
  consistentHash=ConsistentHashHelper.createConsistentHash(configuration,globalCfg.transport().hasTopologyInfo(),Collections.singleton(rpcManager.getAddress()));
}
