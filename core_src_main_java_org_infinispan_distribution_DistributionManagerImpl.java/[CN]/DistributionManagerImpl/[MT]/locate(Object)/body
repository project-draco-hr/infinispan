{
  List<Address> adds=consistentHash.locate(key,replCount);
  if (trace)   log.trace("Located {0} addresses for key {1}.  Repl count is {2}, addresses are {3}",adds.size(),key,replCount,adds);
  return adds;
}
