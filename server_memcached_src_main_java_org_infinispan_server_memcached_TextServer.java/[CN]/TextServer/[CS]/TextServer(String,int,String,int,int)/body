{
  this(host,port,configFile == null ? new DefaultCacheManager().getCache() : new DefaultCacheManager(configFile).getCache(),masterThreads,masterThreads);
  if (configFile == null) {
    log.debug("Using cache manager using configuration defaults");
  }
 else {
    log.debug("Using cache manager configured from {0}",configFile);
  }
}
