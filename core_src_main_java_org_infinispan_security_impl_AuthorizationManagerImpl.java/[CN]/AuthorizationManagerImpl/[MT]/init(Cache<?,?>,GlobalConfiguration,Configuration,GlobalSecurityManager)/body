{
  this.configuration=configuration.security().authorization();
  this.authzHelper=new AuthorizationHelper(globalConfiguration.security(),AuditContext.CACHE,cache.getName(),(ConcurrentMap<CachePrincipalPair,SubjectACL>)globalSecurityManager.globalACLCache());
}
