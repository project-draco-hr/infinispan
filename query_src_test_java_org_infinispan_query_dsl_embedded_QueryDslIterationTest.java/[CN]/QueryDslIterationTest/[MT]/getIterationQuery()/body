{
  QueryFactory qf=Search.getSearchManager(cache).getQueryFactory();
  QueryBuilder<LuceneQuery> queryQueryBuilder=qf.from(User.class).not().having("surname").eq("Blue").toBuilder();
  return queryQueryBuilder.build();
}
