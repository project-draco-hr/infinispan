{
  mc1.set("key","value");
  assertEquals("value",mc1.get("key"));
  assertEquals("value",mc2.get("key"));
  mc2.delete("key");
  assertEquals(null,mc1.get("key"));
  assertEquals(null,mc2.get("key"));
  assertEquals(null,dbServer1.stringTable.getValueByKey("key"));
}
