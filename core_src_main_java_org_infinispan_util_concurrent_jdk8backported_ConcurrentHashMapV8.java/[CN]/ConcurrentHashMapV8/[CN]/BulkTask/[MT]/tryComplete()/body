{
  BulkTask<K,V,?> a=this, s=a;
  for (int c; ; ) {
    if ((c=a.pending) == 0) {
      if ((a=(s=a).parent) == null) {
        s.quietlyComplete();
        break;
      }
    }
 else     if (U.compareAndSwapInt(a,PENDING,c,c - 1))     break;
  }
}
