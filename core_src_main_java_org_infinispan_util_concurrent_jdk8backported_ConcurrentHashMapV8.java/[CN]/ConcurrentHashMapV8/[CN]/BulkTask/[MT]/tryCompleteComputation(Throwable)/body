{
  for (BulkTask<K,V,?> a=this; ; ) {
    BulkTask<K,V,?> p=a.parent;
    if (p == null) {
      if (ex != null)       a.completeExceptionally(ex);
 else       a.quietlyComplete();
      return false;
    }
    a=p;
  }
}
