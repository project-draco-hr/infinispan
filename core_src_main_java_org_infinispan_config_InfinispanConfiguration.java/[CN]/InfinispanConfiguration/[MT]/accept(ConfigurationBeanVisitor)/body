{
  global.accept(v);
  defaultConfiguration.accept(v);
  if (namedCaches != null) {
    for (    Configuration c : namedCaches) {
      c.accept(v);
    }
  }
  v.visitInfinispanConfiguration(this);
}
