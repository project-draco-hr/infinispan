{
  this.context=context;
  this.cacheManagerName=context.getParentResourceComponent().context.getResourceKey();
  this.cacheName=context.getResourceKey();
  if (log.isTraceEnabled())   log.tracef("Start cache component for cache manager %s with cache key %s",cacheManagerName,cacheName);
}
