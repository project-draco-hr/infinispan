{
  config.loaders().clearCacheLoaders().addStore(DummyInMemoryCacheStoreConfigurationBuilder.class).storeName("Store-" + storeCounter.getAndIncrement()).singletonStore().enable().pushStateWhenCoordinator(pushing);
  return config;
}
