{
  if (trace) {
    log.tracef("Release locks for keys=%s. owner=%s",keys,lockOwner);
  }
  if (keys.isEmpty()) {
    return;
  }
  for (  Object key : keys) {
    lockContainer.release(key,lockOwner);
  }
}
