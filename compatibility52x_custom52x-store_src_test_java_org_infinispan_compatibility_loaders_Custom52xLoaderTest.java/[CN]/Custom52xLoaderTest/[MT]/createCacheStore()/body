{
  clearTempDir();
  fcs=new Custom52xCacheStore();
  Custom52xCacheStoreConfig cfg=new Custom52xCacheStoreConfig().fetchPersistentState(true).fsyncMode(getFsyncMode()).location(tmpDirectory).purgeSynchronously(true);
  fcs.init(cfg,getCache(),getMarshaller());
  fcs.start();
  return fcs;
}
