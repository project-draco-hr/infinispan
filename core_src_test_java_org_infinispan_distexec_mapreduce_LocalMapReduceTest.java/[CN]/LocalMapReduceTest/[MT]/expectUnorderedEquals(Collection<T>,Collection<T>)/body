{
  if (h1.size() != h2.size())   return false;
  List<T> clone=new ArrayList<>(h2);
  for (  T next : h1) {
    if (clone.contains(next))     clone.remove(next);
 else     return false;
  }
  return true;
}
