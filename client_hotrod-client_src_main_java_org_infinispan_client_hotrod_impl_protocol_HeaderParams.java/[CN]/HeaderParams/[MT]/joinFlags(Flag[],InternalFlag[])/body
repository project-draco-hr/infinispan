{
  int flagInt=0;
  if (flags != null) {
    for (    Flag flag : flags)     flagInt=flag.getFlagInt() | flagInt;
  }
  if (internalFlags != null) {
    for (    InternalFlag internalFlag : internalFlags) {
      flagInt=internalFlag.getFlagInt() | flagInt;
    }
  }
  return flagInt;
}
