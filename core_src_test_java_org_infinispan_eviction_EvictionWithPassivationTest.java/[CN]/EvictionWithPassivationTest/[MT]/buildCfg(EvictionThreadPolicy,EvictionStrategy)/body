{
  ConfigurationBuilder cfg=new ConfigurationBuilder();
  cfg.persistence().passivation(true).addStore(DummyInMemoryStoreConfigurationBuilder.class).purgeOnStartup(true).eviction().strategy(strategy).threadPolicy(threadPolicy).maxEntries(EVICTION_MAX_ENTRIES).invocationBatching().enable();
  return cfg;
}
