{
  ConfigurationBuilder cfg=new ConfigurationBuilder();
  cfg.loaders().passivation(true).addStore(DummyInMemoryCacheStoreConfigurationBuilder.class).purgeOnStartup(true).eviction().strategy(strategy).threadPolicy(threadPolicy).maxEntries(EVICTION_MAX_ENTRIES).invocationBatching().enable();
  return cfg;
}
