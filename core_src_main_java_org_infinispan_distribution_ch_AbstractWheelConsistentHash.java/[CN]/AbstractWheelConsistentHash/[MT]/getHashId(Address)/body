{
  if (isVirtualNodesEnabled())   throw new IllegalStateException("With virtual nodes enabled each address has more than one hash id");
  for (int i=0; i < positionValues.length; i++) {
    if (positionValues[i].equals(a))     return positionKeys[i];
  }
  return -1;
}
