{
  while (positions.containsKey(positionIndex)) {
    if (positionIndex == Integer.MAX_VALUE)     positionIndex=Integer.MIN_VALUE;
 else     positionIndex=positionIndex + 1;
  }
  positions.put(positionIndex,a);
}
