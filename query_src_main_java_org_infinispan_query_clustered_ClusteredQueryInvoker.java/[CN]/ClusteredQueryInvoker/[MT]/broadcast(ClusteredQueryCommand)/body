{
  FutureTask<Object> localResponse=localInvoke(clusteredQuery);
  List<Object> objects=cast(rpcManager.invokeRemotely(null,clusteredQuery,ResponseMode.SYNCHRONOUS,10000));
  objects.add(localResponse.get());
  return objects;
}
