{
  Executor executor=Executors.newSingleThreadExecutor();
  FutureTask<Object> future=new FutureTask<Object>(new ClusteredQueryCallable(clusteredQuery,localCacheInstance));
  executor.execute(future);
  return future;
}
