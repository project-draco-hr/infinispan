{
  InternalCacheValue sv=getStoredValue(key,ctx);
  persistenceManager.writeToAllStores(new MarshalledEntryImpl(key,sv.getValue(),internalMetadata(sv),marshaller),skipSharedStores(ctx,key,command));
  if (getLog().isTraceEnabled())   getLog().tracef("Stored entry %s under key %s",sv,key);
}
