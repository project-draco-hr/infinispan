{
  if (delegate == null) {
synchronized (this) {
      if (delegate == null) {
        delegate=new BlockingTaskAwareExecutorServiceImpl(factory.getExecutor(executorProperties));
      }
    }
  }
}
