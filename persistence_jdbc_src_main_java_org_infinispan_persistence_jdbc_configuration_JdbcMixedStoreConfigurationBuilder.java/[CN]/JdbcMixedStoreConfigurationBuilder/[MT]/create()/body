{
  return new JdbcMixedStoreConfiguration(purgeOnStartup,fetchPersistentState,ignoreModifications,async.create(),singletonStore.create(),preload,shared,properties,connectionFactory.create(),manageConnectionFactory,batchSize,fetchSize,dialect,binaryTable.create(),stringTable.create(),key2StringMapper,lockConcurrencyLevel,lockAcquisitionTimeout);
}
