{
  final InputStream configFileInputStream=configFileLocation.getInputStream();
  try {
    ConfigurationBuilderHolder parsed=new Parser(Thread.currentThread().getContextClassLoader()).parse(configFileInputStream);
    return new ConfigurationContainer(parsed);
  }
  finally {
    configFileInputStream.close();
  }
}
