{
  if (!enabled && onRehash == null) {
    log.debug("L1 is disabled and L1OnRehash was not defined, disabling it");
    onRehash=false;
  }
  if (onRehash == null)   onRehash=true;
  return new L1Configuration(enabled,invalidationThreshold,lifespan,onRehash);
}
