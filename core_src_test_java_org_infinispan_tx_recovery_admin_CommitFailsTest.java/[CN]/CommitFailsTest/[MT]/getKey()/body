{
  KeyAffinityService kaf=KeyAffinityServiceFactory.newKeyAffinityService(this.cache(2),Executors.newSingleThreadExecutor(),new RndKeyGenerator(),1000);
  Object key=kaf.getKeyForAddress(address(2));
  kaf.stop();
  return key;
}
