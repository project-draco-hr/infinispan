{
  while (completed.get() < scheduled.get()) {
synchronized (this) {
      try {
        wait(100);
      }
 catch (      InterruptedException e) {
        Thread.currentThread().interrupt();
        return;
      }
    }
  }
}
