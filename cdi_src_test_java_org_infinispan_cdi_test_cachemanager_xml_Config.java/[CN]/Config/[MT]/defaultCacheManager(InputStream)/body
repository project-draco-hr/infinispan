{
  EmbeddedCacheManager externalCacheContainerManager=new DefaultCacheManager(xml);
  externalCacheContainerManager.defineConfiguration("quick-very-large",new Configuration().fluent().expiration().wakeUpInterval(1l).build());
  return externalCacheContainerManager;
}
