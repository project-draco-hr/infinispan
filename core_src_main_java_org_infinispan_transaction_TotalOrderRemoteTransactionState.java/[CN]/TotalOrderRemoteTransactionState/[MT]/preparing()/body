{
  if (log.isTraceEnabled()) {
    log.tracef("[%s] Current status is %s, setting status to: PREPARING",globalTransaction.globalId(),transactionState);
  }
  transactionState.add(State.PREPARING);
}
