{
  Number value=(Number)srcRow[pos];
  if (value != null) {
    value=widen(value);
    Number sum=(Number)accRow[pos];
    if (sum == null) {
      accRow[pos]=value;
    }
 else {
      accRow[pos]=add(value,sum);
    }
  }
}
