{
  Number value=(Number)srcRow[pos];
  if (value != null) {
    Number sum=(Number)destRow[pos];
    if (sum == null) {
      destRow[pos]=value;
    }
 else {
      destRow[pos]=add(value,sum);
    }
  }
}
