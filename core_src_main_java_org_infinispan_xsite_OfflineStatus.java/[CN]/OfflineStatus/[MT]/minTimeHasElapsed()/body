{
  if (takeOffline.minTimeToWait() <= 0)   throw new IllegalStateException("Cannot invoke this method if minTimeToWait is not enabled");
  long millis=millisSinceFirstFailure();
  if (millis >= takeOffline.minTimeToWait()) {
    log.tracef("The minTimeToWait hasn't passed: minTime=%s, timeSinceFirstFailure=%s",takeOffline.minTimeToWait(),millis);
    return true;
  }
  return false;
}
