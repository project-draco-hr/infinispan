{
  ConfigurationBuilder configuration=getDefaultStandaloneCacheConfig(true);
  configuration.deadlockDetection().enable().locking().useLockStriping(false).jmxStatistics().enable();
  return configuration;
}
