{
  if (version.equals(ConfigurationProperties.PROTOCOL_VERSION_10))   return CODEC_10;
 else   if (version.equals(ConfigurationProperties.PROTOCOL_VERSION_11))   return CODEC_11;
 else   throw new IllegalArgumentException("Invalid Hot Rod protocol version");
}
