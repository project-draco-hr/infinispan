{
  if (argument == null) {
    candidates.addAll(all);
  }
 else {
    String prefix=argument.getValue();
    for (    String s : all) {
      if (s.startsWith(prefix)) {
        candidates.add(s);
      }
    }
  }
}
