{
  for (; ; ) {
    Entry entry=table.putIfAbsent(key,new Entry(file,offset));
    if (entry != null) {
synchronized (entry) {
        if (entry.isRemoved()) {
          continue;
        }
        entry.update(file,offset);
        return;
      }
    }
 else {
      return;
    }
  }
}
