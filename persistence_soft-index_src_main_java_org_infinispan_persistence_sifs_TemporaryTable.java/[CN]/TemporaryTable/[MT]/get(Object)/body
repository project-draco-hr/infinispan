{
  Entry entry=table.get(key);
  if (entry == null) {
    return null;
  }
  int file, offset;
synchronized (entry) {
    file=entry.getFile();
    offset=entry.getOffset();
  }
  return new EntryPosition(file,offset);
}
