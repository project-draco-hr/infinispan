{
  storeBuilder=TestCacheManagerFactory.getDefaultCacheConfiguration(false).loaders().addLoader(JdbcMixedCacheStoreConfigurationBuilder.class).purgeSynchronously(true);
  storeBuilder.simpleConnection().connectionUrl("url").driverClass("driver");
}
