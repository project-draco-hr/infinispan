{
  async.validate();
  singletonStore.validate();
  if (!loaders().shared() && !fetchPersistentState && !purgeOnStartup&& getBuilder().clustering().cacheMode().isClustered())   log.staleEntriesWithoutFetchPersistentStateOrPurgeOnStartup();
}
