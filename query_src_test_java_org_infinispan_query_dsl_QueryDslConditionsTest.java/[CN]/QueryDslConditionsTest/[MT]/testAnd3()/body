{
  QueryFactory qf=Search.getSearchManager(cache).getQueryFactory();
  Query q3=qf.from(User.class).having("gender").eq(User.Gender.MALE).and().having("gender").eq(User.Gender.FEMALE).toBuilder().build();
  List<User> list3=q3.list();
  assertEquals(0,list3.size());
}
