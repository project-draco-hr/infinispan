{
  QueryFactory qf=Search.getSearchManager(cache).getQueryFactory();
  Query q=qf.from(User.class).having("id").in(Collections.EMPTY_SET).toBuilder().build();
  List<User> list=q.list();
  assertEquals(0,list.size());
}
