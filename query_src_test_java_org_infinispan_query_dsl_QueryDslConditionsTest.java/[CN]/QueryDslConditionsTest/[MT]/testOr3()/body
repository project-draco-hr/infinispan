{
  QueryFactory qf=Search.getSearchManager(cache).getQueryFactory();
  Query q1=qf.from(User.class).having("gender").eq(User.Gender.MALE).or().having("gender").eq(User.Gender.FEMALE).toBuilder().build();
  List<User> list1=q1.list();
  assertEquals(3,list1.size());
}
