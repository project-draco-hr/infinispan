{
  this.transportFactory=transportFactory;
  this.cacheNameBytes=cacheName.equals(BasicCacheContainer.DEFAULT_CACHE_NAME) ? DEFAULT_CACHE_NAME_BYTES : cacheName.getBytes(HOTROD_STRING_CHARSET);
  this.topologyId=topologyId;
  this.forceReturnValue=forceReturnValue;
  this.codec=codec;
}
