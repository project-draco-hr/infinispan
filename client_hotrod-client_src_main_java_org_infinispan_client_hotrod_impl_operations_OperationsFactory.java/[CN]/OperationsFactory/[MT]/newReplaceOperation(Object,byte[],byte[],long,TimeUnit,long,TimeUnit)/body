{
  return new ReplaceOperation<>(codec,transportFactory,key,keyBytes,cacheNameBytes,topologyId,flags(lifespan,maxIdle),clientIntelligence,values,lifespan,lifespanTimeUnit,maxIdle,maxIdleTimeUnit);
}
