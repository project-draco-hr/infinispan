{
  return new PutIfAbsentOperation<>(codec,transportFactory,key,keyBytes,cacheNameBytes,topologyId,flags(lifespan,maxIdleTime),clientIntelligence,value,lifespan,lifespanUnit,maxIdleTime,maxIdleTimeUnit);
}
