{
  mc.writeln("set " + KEY_A + " 0 2592000 1");
  mc.writeln("A");
  mc.flush();
  assertEquals("STORED",mc.readln());
  assertEquals("A",mc.get(KEY_A));
  Thread.sleep(2000);
  assertEquals("A",mc.get(KEY_A));
}
