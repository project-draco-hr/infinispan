{
  splitCluster(new int[]{0,1},new int[]{2,3});
  try (EntryIterable iterable=cache(0).getAdvancedCache().withFlags(Flag.CACHE_MODE_LOCAL).filterEntries(AcceptAllKeyValueFilter.getInstance())){
    iterable.iterator();
  }
 }
