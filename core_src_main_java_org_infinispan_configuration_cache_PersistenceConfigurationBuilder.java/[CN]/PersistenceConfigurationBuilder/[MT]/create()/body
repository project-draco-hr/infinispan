{
  List<StoreConfiguration> loaders=new LinkedList<StoreConfiguration>();
  for (  StoreConfigurationBuilder<?,?> loader : stores)   loaders.add(loader.create());
  return new PersistenceConfiguration(passivation,loaders);
}
