{
  return new ResultProcessor<Object,Object>(){
    @Override public Object process(    Object result){
      result=result instanceof ProtobufValueWrapper ? ((ProtobufValueWrapper)result).getBinary() : result;
      if (in != null) {
        result=in.process(result);
      }
      return result;
    }
  }
;
}
