{
  if (aliasToEntityType.containsKey(property.getText())) {
    return normalizeQualifiedRoot(property);
  }
  return normalizeProperty(new ProtobufValueWrapperTypeDescriptor(targetType),Collections.<String>emptyList(),property.getText());
}
