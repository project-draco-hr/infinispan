{
  Modification mod=state.get(key);
  if (mod != null) {
switch (mod.getType()) {
case REMOVE:
case CLEAR:
      return null;
case STORE:
    InternalCacheEntry ice=((Store)mod).getStoredEntry();
  if (ice.isExpired())   return null;
return ice;
}
}
return super.load(key);
}
