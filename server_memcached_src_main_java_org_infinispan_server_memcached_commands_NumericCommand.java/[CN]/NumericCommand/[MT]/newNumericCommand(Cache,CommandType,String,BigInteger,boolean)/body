{
switch (type) {
case INCR:
    return new IncrementCommand(cache,type,key,value,noReply);
case DECR:
  return new DecrementCommand(cache,type,key,value,noReply);
default :
throw new StreamCorruptedException("Unable to build storage command for type: " + type);
}
}
