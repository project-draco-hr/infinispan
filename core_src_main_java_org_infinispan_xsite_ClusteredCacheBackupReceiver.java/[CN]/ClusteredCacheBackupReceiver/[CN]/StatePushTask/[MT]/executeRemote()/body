{
  final RpcManager rpcManager=cache.getAdvancedCache().getRpcManager();
  remoteFuture=rpcManager.invokeRemotelyAsync(Collections.singletonList(address),newStatePushCommand(cache,chunk),rpcManager.getDefaultRpcOptions(true));
}
