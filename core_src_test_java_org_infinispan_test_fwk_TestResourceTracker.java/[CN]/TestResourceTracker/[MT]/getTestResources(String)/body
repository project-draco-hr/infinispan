{
  return testResources.computeIfAbsent(testName,new EquivalentConcurrentHashMapV8.Fun<String,TestResources>(){
    @Override public TestResources apply(    String key){
      return new TestResources(getSimpleName(testName));
    }
  }
);
}
