{
  QueryFactory qf=getQueryFactory();
  Query q=qf.from(getModelFactory().getUserImplClass()).select(Expression.min("addresses.street")).having("name").eq("Spider").toBuilder().build();
  List<Object[]> list=q.list();
  assertEquals(list.get(0)[0],"Bond Street");
}
