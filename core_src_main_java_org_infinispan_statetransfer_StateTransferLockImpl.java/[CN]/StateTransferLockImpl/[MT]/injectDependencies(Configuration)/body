{
  pessimisticLocking=config.transaction().lockingMode() == LockingMode.PESSIMISTIC;
  isSync=config.clustering().cacheMode().isSynchronous();
  lockTimeout=config.clustering().cacheMode().isDistributed() ? config.clustering().hash().rehashWait() : config.clustering().stateTransfer().timeout();
}
