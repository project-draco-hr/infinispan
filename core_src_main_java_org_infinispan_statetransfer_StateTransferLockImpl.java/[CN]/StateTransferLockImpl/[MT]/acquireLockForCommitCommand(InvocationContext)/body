{
  if (acquireLockForCommitNoWait())   return true;
  if (!ctx.isOriginLocal())   return false;
synchronized (lock) {
    while (true) {
      lock.wait();
      if (acquireLockForCommitNoWait())       return true;
    }
  }
}
