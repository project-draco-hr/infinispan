{
  if (acquireLockForCommitNoWait())   return true;
  if (!ctx.isOriginLocal() && isSync)   return false;
synchronized (lock) {
    while (true) {
      if (acquireLockForCommitNoWait())       return true;
      lock.wait();
    }
  }
}
