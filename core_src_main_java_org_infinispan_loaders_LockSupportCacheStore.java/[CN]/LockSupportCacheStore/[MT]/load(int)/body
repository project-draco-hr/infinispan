{
  if (maxEntries < 0) {
    return loadAll();
  }
  boolean success=acquireGlobalLock(false);
  try {
    return loadLockSafe(maxEntries);
  }
  finally {
    if (success) {
      releaseGlobalLock(false);
    }
  }
}
