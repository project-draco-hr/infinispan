{
  for (  Builder<?> validatable : asList(evictionThreadPool,listenerThreadPool,replicationQueueThreadPool,persistenceThreadPool,globalJmxStatistics,transport,serialization,shutdown,site)) {
    validatable.validate();
  }
  for (  Builder<?> m : modules) {
    m.validate();
  }
}
