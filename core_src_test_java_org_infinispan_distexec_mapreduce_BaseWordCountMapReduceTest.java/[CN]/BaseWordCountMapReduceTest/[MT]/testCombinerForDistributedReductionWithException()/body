{
  MapReduceTask<String,String,String,Integer> task=invokeMapReduce(null);
  task.combinedWith(new Reducer<String,Integer>(){
    @Override public Integer reduce(    String reducedKey,    Iterator<Integer> iter){
      int a=4 / 0;
      return null;
    }
  }
);
  task.execute();
}
