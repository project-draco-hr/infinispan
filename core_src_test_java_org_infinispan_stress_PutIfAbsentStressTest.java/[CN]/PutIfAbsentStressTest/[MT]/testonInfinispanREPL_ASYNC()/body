{
  System.out.println("Running test on Infinispan, REPL_ASYNC:");
  Configuration c=new Configuration().fluent().mode(Configuration.CacheMode.REPL_ASYNC).build();
  testConcurrentLockingOnMultipleManagers(c);
}
