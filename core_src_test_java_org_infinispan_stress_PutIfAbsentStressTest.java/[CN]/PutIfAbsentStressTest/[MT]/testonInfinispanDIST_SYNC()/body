{
  System.out.println("Running test on Infinispan, DIST_SYNC:");
  Configuration c=new Configuration().fluent().mode(Configuration.CacheMode.DIST_SYNC).build();
  testConcurrentLockingOnMultipleManagers(c);
}
