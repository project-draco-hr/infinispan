{
  System.out.println("Running test on Infinispan, REPL_SYNC:");
  Configuration c=new Configuration().fluent().mode(Configuration.CacheMode.REPL_SYNC).build();
  testConcurrentLockingOnMultipleManagers(c);
}
