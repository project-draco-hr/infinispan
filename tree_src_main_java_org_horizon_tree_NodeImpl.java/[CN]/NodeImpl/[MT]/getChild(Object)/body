{
  startAtomic();
  try {
    if (hasChild(name))     return new NodeImpl<K,V>(Fqn.fromRelativeElements(fqn,name),cache,batchContainer,icc);
 else     return null;
  }
  finally {
    endAtomic();
  }
}
