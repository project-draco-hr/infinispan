{
  setFlagsOnContext(flags);
  try {
    startAtomic();
    try {
      Node<K,V> node=getNode(fqn);
      if (node == null)       return null;
 else       return node.getData();
    }
  finally {
      endAtomic();
    }
  }
  finally {
    tcc.suspend();
  }
}
