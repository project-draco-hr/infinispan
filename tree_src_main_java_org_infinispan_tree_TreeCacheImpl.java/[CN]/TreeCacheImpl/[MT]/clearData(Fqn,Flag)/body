{
  setFlagsOnContext(flags);
  try {
    startAtomic();
    try {
      Node<K,V> node=getNode(fqn);
      if (node != null)       node.clearData();
    }
  finally {
      endAtomic();
    }
  }
  finally {
    tcc.suspend();
  }
}
