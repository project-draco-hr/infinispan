{
  if (!state.startAllowed()) {
    if (state.needToDestroyFailedCache())     destroy();
    if (state.needToInitializeBeforeStart()) {
      state=ComponentStatus.INITIALIZING;
      rewire();
    }
 else     return;
  }
  try {
    internalStart();
  }
 catch (  Throwable t) {
    handleLifecycleTransitionFailure(t);
  }
}
