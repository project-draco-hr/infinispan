{
  maxNumberOfKeys.set(address2key.keySet().size() * bufferSize);
  exitingNumberOfKeys.set(0);
  if (log.isTraceEnabled()) {
    log.tracef("resetNumberOfKeys ends with: maxNumberOfKeys=%d, exitingNumberOfKeys=%s",maxNumberOfKeys,exitingNumberOfKeys);
  }
}
