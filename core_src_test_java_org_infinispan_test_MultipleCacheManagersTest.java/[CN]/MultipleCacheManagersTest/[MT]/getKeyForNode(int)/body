{
  ExecutorService ex=Executors.newSingleThreadExecutor();
  KeyAffinityService<Object> kas=KeyAffinityServiceFactory.newKeyAffinityService(cache(nodeIndex),ex,new RndKeyGenerator(),5,true);
  try {
    return kas.getKeyForAddress(address(nodeIndex));
  }
  finally {
    ex.shutdown();
  }
}
