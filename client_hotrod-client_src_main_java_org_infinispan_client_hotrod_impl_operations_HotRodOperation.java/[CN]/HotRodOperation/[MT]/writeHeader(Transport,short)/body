{
  HeaderParams params=new HeaderParams().opCode(operationCode).cacheName(cacheName).flags(flags).internalFlags(internalFlags).clientIntel(CLIENT_INTELLIGENCE_HASH_DISTRIBUTION_AWARE).topologyId(topologyId).txMarker(NO_TX);
  return codec.writeHeader(transport,params);
}
