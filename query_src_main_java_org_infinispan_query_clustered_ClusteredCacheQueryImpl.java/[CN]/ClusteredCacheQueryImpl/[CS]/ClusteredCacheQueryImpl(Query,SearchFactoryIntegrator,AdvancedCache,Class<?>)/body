{
  super(luceneQuery,searchFactory,cache,classes);
  hSearchQuery=new ISPNQuery((SearchFactoryImplementor)searchFactory);
  hSearchQuery.luceneQuery(luceneQuery).targetedEntities(Arrays.asList(classes));
}
