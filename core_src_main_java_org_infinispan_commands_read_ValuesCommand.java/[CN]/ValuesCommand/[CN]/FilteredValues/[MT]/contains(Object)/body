{
  for (  CacheEntry e : lookedUpEntries.values()) {
    if (o.equals(e.getValue())) {
      if (e.isRemoved()) {
        return false;
      }
 else {
        return true;
      }
    }
  }
  return values.contains(o);
}
