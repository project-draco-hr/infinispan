{
  String systemProperty=System.getProperty(property);
  if (systemProperty == null) {
    throw new AssertionFailure("The test setup requires the system property '" + property + "' to be set. Check your environment!");
  }
  return systemProperty;
}
