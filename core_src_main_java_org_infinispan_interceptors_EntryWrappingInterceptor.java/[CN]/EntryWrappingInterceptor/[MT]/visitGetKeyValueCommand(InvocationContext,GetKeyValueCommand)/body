{
  try {
    checkIfKeyRead(ctx,command.getKey(),command);
    entryFactory.wrapEntryForReading(ctx,command.getKey());
    return invokeNextInterceptor(ctx,command);
  }
  finally {
    if (!ctx.isInTxScope())     commitContextEntries(ctx,command);
  }
}
