{
  try {
    entryFactory.wrapEntryForReading(ctx,command.getKey());
    return invokeNextInterceptor(ctx,command);
  }
  finally {
    if (!ctx.isInTxScope())     commitContextEntries(ctx,command.hasFlag(Flag.SKIP_OWNERSHIP_CHECK));
  }
}
