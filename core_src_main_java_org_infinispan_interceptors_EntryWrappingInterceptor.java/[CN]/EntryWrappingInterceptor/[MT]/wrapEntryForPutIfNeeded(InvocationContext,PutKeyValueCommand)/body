{
  if (shouldWrap(command.getKey(),ctx,command)) {
    entryFactory.wrapEntryForPut(ctx,command.getKey(),null,!command.isConditional(),command,command.hasFlag(Flag.IGNORE_RETURN_VALUES) && !command.isConditional());
  }
}
