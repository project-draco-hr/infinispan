{
  isUsingLockDelegation=!cacheConfiguration.transaction().transactionMode().isTransactional() && cacheConfiguration.clustering().cacheMode().isDistributed();
  isInvalidation=cacheConfiguration.clustering().cacheMode().isInvalidation();
}
