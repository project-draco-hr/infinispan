{
  Object key=entry.getKey();
  if (trace)   log.trace("store() " + key);
  try {
    tree.insert(key,entry,true);
    if (entry.canExpire())     addNewExpiry(entry);
  }
 catch (  IOException e) {
    throw new CacheLoaderException(e);
  }
}
