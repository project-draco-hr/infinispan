{
  if (rsps != null) {
    for (    Object rsp : rsps) {
      if (rsp != null && rsp instanceof Throwable) {
        if (log.isDebugEnabled())         log.debug("Received Throwable from remote cache",(Throwable)rsp);
        throw (Throwable)rsp;
      }
    }
  }
}
