{
  if (validKey(key)) {
    return "S:" + key.toString();
  }
 else   throw new IllegalArgumentException("Indexing only works with entries keyed on Strings - you passed in a " + key.getClass().toString());
}
