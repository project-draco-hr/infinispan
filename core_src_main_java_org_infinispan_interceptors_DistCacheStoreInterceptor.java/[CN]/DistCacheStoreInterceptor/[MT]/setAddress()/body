{
  this.address=transport.getAddress();
  this.isUsingLockDelegation=cacheConfiguration.locking().supportsConcurrentUpdates() && !cacheConfiguration.transaction().transactionMode().isTransactional();
}
