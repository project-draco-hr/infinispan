{
  ClassLoader cl=FileLookup.class.getClassLoader();
  if (cl.getClass().getName().equals("org.osgi.framework.BundleReference"))   return new OsgiFileLookup();
 else   return new DefaultFileLookup();
}
