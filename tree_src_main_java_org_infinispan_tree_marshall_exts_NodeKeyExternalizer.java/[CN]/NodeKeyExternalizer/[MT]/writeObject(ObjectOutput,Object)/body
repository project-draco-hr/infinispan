{
  NodeKey key=(NodeKey)object;
  output.writeObject(key.getFqn());
  byte type=0;
switch (key.getContents()) {
case DATA:
    type=DATA_BYTE;
  break;
case STRUCTURE:
type=STRUCTURE_BYTE;
break;
}
output.write(type);
}
