{
  final int numOwners=baseCH.getNumOwners();
  DefaultConsistentHash updated=new DefaultConsistentHash(baseCH.getHashFunction(),numOwners,1,newMembers,null,new List[]{createOwnersCollection(baseCH.getMembers(),numOwners)});
  return baseCH.equals(updated) ? baseCH : updated;
}
