{
  if (keys == null || keys.isEmpty()) {
    if (singleKey == null)     return new Object[]{globalTx,cacheName,(byte)1};
 else     return new Object[]{globalTx,cacheName,(byte)2,singleKey};
  }
  return new Object[]{globalTx,cacheName,(byte)3,keys};
}
