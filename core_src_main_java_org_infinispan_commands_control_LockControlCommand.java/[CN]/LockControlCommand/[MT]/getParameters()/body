{
  if (keys == null || keys.isEmpty()) {
    if (singleKey == null)     return new Object[]{globalTx,cacheName,unlock,(byte)1,flags};
 else     return new Object[]{globalTx,cacheName,unlock,(byte)2,singleKey,flags};
  }
  return new Object[]{globalTx,cacheName,unlock,(byte)3,keys,flags};
}
