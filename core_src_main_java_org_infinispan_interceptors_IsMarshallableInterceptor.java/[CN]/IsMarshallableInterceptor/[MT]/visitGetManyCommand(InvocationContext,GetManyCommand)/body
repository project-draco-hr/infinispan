{
  if (isStoreAsBinary()) {
    for (    Object key : command.getKeys()) {
      if (getMightGoRemote(ctx,key,command))       checkMarshallable(key);
    }
  }
  return super.visitGetManyCommand(ctx,command);
}
