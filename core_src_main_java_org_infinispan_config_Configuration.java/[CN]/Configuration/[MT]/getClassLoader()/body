{
  if (cl != null)   return cl;
 else   if (cl == null && globalConfiguration != null)   return globalConfiguration.getClassLoader();
 else   return Thread.currentThread().getContextClassLoader();
}
