{
  return clustering.hash.consistentHashClass != null && !clustering.hash.consistentHashClass.equals(DefaultConsistentHash.class.getName()) && !clustering.hash.consistentHashClass.equals(TopologyAwareConsistentHash.class.getName());
}
