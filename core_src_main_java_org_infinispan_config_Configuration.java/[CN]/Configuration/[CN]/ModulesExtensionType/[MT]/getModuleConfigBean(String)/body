{
  if (moduleName == null)   throw new IllegalArgumentException("module name cannot be null");
  for (  ModuleConfigurationBean c : moduleList) {
    if (moduleName.equalsIgnoreCase(c.name))     return c;
  }
  return null;
}
