{
  Metadata metadata=new EmbeddedMetadata.Builder().lifespan(cacheImplementation.defaultLifespan,MILLISECONDS).maxIdle(cacheImplementation.defaultMaxIdleTime,MILLISECONDS).build();
  return cacheImplementation.replaceAsync(key,value,metadata,flags,classLoader.get());
}
