{
  if (this.classLoader == null)   return super.with(classLoader);
 else   throw new UnsupportedOperationException("The purpose of a DecoratedCache when used with a ClassLoader is to avoid calling the with(ClassLoader) API.");
}
