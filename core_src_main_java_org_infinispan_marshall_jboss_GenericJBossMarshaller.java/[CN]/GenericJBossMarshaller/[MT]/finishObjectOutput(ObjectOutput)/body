{
  try {
    if (log.isTraceEnabled())     log.trace("Stop marshaller@%s",Integer.toHexString(System.identityHashCode(oo)));
    ((org.jboss.marshalling.Marshaller)oo).finish();
  }
 catch (  IOException ignored) {
  }
}
