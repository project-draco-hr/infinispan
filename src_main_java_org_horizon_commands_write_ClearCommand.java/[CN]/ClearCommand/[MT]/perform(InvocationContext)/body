{
  for (  CacheEntry e : ctx.getLookedUpEntries().values()) {
    if (e instanceof MVCCEntry) {
      MVCCEntry me=(MVCCEntry)e;
      me.setRemoved(true);
      me.setValid(false);
    }
  }
  return null;
}
