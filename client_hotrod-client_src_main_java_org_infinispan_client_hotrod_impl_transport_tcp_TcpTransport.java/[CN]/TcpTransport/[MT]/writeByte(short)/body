{
  try {
    socketOutputStream.write(toWrite);
    if (trace)     log.trace("Wrote byte " + toWrite);
  }
 catch (  IOException e) {
    throw new TransportException("Problems writing data to stream",e);
  }
}
