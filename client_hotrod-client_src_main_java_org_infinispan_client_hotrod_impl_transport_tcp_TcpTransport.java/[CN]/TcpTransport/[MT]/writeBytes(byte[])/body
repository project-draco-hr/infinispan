{
  try {
    socketOutputStream.write(toAppend);
    if (trace)     log.tracef("Wrote %d bytes",toAppend.length);
  }
 catch (  IOException e) {
    throw new TransportException("Problems writing data to stream",e);
  }
}
