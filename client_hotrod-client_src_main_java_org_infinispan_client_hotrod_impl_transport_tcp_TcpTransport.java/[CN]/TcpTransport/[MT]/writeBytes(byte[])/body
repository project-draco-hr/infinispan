{
  try {
    socket.getOutputStream().write(toAppend);
    if (trace)     log.trace("Wrote " + toAppend.length + " bytes");
  }
 catch (  IOException e) {
    throw new TransportException("Problems writing data to stream",e);
  }
}
