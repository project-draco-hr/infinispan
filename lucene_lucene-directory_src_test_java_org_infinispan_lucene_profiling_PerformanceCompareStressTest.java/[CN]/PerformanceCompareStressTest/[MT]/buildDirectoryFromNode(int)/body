{
  EmbeddedCacheManager cm=cacheManagers.get(node);
  return DirectoryBuilder.newDirectoryInstance(cm.getCache("index_metadata"),cm.getCache("index_data"),cm.getCache("index_locks"),indexName).chunkSize(CHUNK_SIZE).create();
}
