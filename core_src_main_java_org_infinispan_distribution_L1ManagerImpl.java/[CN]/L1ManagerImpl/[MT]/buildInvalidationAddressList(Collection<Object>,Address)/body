{
  Collection<Address> addresses=new HashSet<Address>();
  for (  Object key : keys) {
    Collection<Address> as=requestors.get(key);
    if (as != null)     addresses.addAll(as);
  }
  if (origin != null)   addresses.remove(origin);
  return addresses;
}
