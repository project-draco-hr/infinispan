{
  ConfigurationBuilder c=getDefaultClusteredCacheConfig(CacheMode.REPL_SYNC);
  c.loaders().passivation(true).shared(true).addStore(DummyInMemoryCacheStoreConfigurationBuilder.class).storeName("ReplicatedSharedEvictingLoaderTest");
  createCluster(c,2);
  waitForClusterToForm();
}
