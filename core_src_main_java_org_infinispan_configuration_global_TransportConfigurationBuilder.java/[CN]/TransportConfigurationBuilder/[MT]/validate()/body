{
  for (  Builder<?> validatable : asList(transportThreadPool,remoteCommandThreadPool,totalOrderThreadPool)) {
    validatable.validate();
  }
  if (clusterName == null) {
    throw new CacheConfigurationException("Transport clusterName cannot be null");
  }
}
