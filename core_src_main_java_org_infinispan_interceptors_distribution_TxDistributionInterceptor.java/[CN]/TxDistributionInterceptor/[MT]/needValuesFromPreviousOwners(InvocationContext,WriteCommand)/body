{
  if (ctx.isOriginLocal()) {
    if (isNeedReliableReturnValues(command) || command.isConditional())     return true;
  }
  return shouldFetchRemoteValuesForWriteSkewCheck(ctx,command) || command.hasFlag(Flag.DELTA_WRITE);
}
