{
  if (ctx.isOriginLocal() && !skipRemoteGet || command.isConditional() || command.hasFlag(Flag.DELTA_WRITE) || shouldFetchRemoteValuesForWriteSkewCheck(ctx,command))   remoteGetBeforeWrite(ctx,command,recipientGenerator);
  return invokeNextInterceptor(ctx,command);
}
