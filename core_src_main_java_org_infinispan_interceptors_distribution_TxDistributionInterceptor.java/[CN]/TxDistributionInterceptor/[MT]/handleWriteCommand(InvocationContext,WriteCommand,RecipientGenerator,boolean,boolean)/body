{
  if (ctx.isOriginLocal() && !skipRemoteGet || shouldFetchRemoteValuesForWriteSkewCheck(ctx,command))   remoteGetBeforeWrite(ctx,command,recipientGenerator);
  if (isLocalModeForced(command)) {
    return invokeNextInterceptor(ctx,command);
  }
  return invokeNextInterceptor(ctx,command);
}
