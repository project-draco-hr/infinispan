{
  if (ctx.isOriginLocal() && !skipRemoteGet)   remoteGetBeforeWrite(ctx,command,recipientGenerator);
  if (isLocalModeForced(command)) {
    return invokeNextInterceptor(ctx,command);
  }
  return invokeNextInterceptor(ctx,command);
}
