{
  if (cacheContainers != null) {
    for (    EmbeddedCacheManager cm : cacheContainers) {
      try {
        clearContent(cm);
      }
  finally {
        if (cm != null)         cm.stop();
      }
    }
  }
}
