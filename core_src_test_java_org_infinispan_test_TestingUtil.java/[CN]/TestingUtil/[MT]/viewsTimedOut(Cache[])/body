{
  int length=caches.length;
  List<List<Address>> allViews=new ArrayList<List<Address>>(length);
  for (int i=0; i < length; i++)   allViews.add(caches[i].getCacheManager().getMembers());
  throw new RuntimeException(String.format("Timed out before caches had complete views.  Expected %d members in each view.  Views are as follows: %s",caches.length,allViews));
}
