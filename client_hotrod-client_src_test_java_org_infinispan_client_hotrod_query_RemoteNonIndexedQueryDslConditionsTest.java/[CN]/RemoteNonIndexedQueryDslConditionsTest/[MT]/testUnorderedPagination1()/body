{
  QueryFactory qf=Search.getQueryFactory(remoteCache);
  Query q=qf.from(User.class).maxResults(0).build();
  List<User> list=q.list();
  assertEquals(3,q.getResultSize());
  assertEquals(0,list.size());
}
