{
  EmsConnection conn=getConnection();
  try {
    conn.refresh();
    EmsBean bean=conn.getBean(objectName);
    if (bean != null)     bean.refreshAttributes();
    return AvailabilityType.UP;
  }
 catch (  Exception e) {
    return AvailabilityType.DOWN;
  }
}
