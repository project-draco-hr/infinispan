{
  if (failedServers == null) {
    failedServers=new HashSet<SocketAddress>();
  }
  if (log.isTraceEnabled())   log.tracef("Add %s to failed servers",address);
  failedServers.add(address);
  return failedServers;
}
