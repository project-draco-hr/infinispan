{
  RequestBalancingStrategy balancingStrategy=Util.getInstance(configuration.balancingStrategy());
  if (balancingStrategy instanceof FailoverRequestBalancingStrategy)   return (FailoverRequestBalancingStrategy)balancingStrategy;
 else   return new FailoverToRequestBalancingStrategyDelegate(balancingStrategy);
}
