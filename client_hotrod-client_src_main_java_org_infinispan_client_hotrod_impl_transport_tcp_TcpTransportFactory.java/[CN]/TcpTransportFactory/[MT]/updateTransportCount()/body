{
synchronized (lock) {
    int maxActive=connectionPool.getMaxActive();
    if (maxActive > 0) {
      transportCount=Math.max(maxActive * servers.size(),maxActive);
    }
 else {
      transportCount=10 * servers.size();
    }
  }
}
