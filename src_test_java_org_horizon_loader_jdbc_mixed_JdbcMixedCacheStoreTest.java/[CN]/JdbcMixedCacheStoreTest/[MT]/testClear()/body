{
  cacheStore.store(new StoredEntry("String","someValue"));
  assertRowCounts(0,1);
  cacheStore.store(new StoredEntry(MIRCEA,"value"));
  assertRowCounts(1,1);
  cacheStore.clear();
  assertRowCounts(0,0);
}
