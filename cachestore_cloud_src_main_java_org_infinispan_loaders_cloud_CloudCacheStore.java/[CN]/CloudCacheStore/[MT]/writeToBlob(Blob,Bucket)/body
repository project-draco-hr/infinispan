{
  try {
    final byte[] payloadBuffer=marshaller.objectToByteBuffer(bucket);
    if (cfg.isCompress())     blob.setPayload(compress(payloadBuffer));
 else     blob.setPayload(payloadBuffer);
  }
 catch (  IOException e) {
    throw new CacheLoaderException(e);
  }
}
