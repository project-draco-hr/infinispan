{
  String bucketName=bucket.getBucketName();
  if (bucketName.startsWith("-"))   bucketName=bucketName.replace("-","A");
 else   if (bucketName.startsWith("A"))   bucketName=bucketName.replace("A","-");
  return bucketName;
}
