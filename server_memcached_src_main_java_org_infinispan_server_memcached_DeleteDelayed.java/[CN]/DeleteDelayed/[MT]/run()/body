{
  try {
    while (!Thread.currentThread().isInterrupted()) {
      DeleteDelayedEntry entry=queue.take();
      cache.remove(entry.key);
    }
  }
 catch (  InterruptedException e) {
    log.debug("Interrupted, so allow thread to exit");
  }
}
