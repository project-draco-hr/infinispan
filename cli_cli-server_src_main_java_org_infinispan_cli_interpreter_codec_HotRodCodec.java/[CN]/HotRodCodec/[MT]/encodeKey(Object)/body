{
  if (key != null) {
    try {
      return new ByteArrayKey(marshaller.objectToByteBuffer(key));
    }
 catch (    Exception e) {
      throw log.keyEncodingFailed(e,this.getName());
    }
  }
 else {
    return null;
  }
}
