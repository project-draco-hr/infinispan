{
switch (type) {
case GET:
    return new GetCommand(cache,type,params);
case GETS:
  return new GetsCommand(cache,type,params);
default :
throw new IllegalStateException("Unable to build storage command for type: " + type);
}
}
