{
  if (cr != null && cr.getComponent(Configuration.class) != null)   return cr.getComponent(Configuration.class).getClassLoader();
 else   if (Thread.currentThread().getContextClassLoader() != null)   return Thread.currentThread().getContextClassLoader();
 else   return null;
}
