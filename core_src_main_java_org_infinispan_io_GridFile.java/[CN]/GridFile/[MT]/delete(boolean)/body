{
  if (!exists())   return false;
  if (isDirectory() && hasChildren())   return false;
  fs.remove(getAbsolutePath(),synchronous);
  if (synchronous)   metadataCache.withFlags(FORCE_SYNCHRONOUS).remove(getAbsolutePath());
 else   metadataCache.remove(getAbsolutePath());
  return true;
}
