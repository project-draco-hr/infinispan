{
  String[] files=_list(filter);
  if (files == null)   return new File[0];
  File[] retval=new File[files.length];
  for (int i=0; i < files.length; i++)   retval[i]=new GridFile(files[i],metadataCache,chunk_size,fs);
  return retval;
}
