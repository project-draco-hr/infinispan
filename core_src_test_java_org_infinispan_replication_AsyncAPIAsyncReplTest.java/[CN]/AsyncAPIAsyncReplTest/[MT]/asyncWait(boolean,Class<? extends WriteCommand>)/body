{
  if (tx) {
    if (cmds == null || cmds.length == 0)     rl.expectAnyWithTx();
 else     rl.expectWithTx(cmds);
  }
 else {
    if (cmds == null || cmds.length == 0)     rl.expectAny();
 else     rl.expect(cmds);
  }
  rl.waitForRpc();
}
