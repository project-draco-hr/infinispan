{
  if (trace)   log.trace("Executing command: " + command + ".");
  Object ret=command.perform(ctx);
  if (ret != null) {
    if (command.isReturnEntry())     notifyCacheEntryVisit(ctx,command,((CacheEntry)ret).getValue());
 else     notifyCacheEntryVisit(ctx,command,ret);
  }
  return ret;
}
