{
  if (trace)   log.trace("Executing command: " + command + ".");
  Object ret=command.perform(ctx);
  if (ret != null)   notifyCacheEntryVisit(ctx,command,((CacheEntry)ret).getValue());
  return ret;
}
