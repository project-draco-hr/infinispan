{
  EmbeddedCacheManager cm=TestCacheManagerFactory.createLocalCacheManager(true);
  cm.getDefaultConfiguration().fluent().transaction().transactionManagerLookupClass(RecoveryDummyTransactionManagerLookup.class).recovery().locking().useLockStriping(false);
  return cm;
}
