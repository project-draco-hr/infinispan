{
  EmbeddedCacheManager cm=TestCacheManagerFactory.createLocalCacheManager(true);
  cm.getDefaultConfiguration().fluent().transaction().transactionManagerLookupClass(DummyTransactionManagerLookup.class).recovery().locking().useLockStriping(false);
  return cm;
}
