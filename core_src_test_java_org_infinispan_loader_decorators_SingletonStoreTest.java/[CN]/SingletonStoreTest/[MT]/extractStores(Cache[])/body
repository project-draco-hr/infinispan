{
  SingletonStore[] stores=new SingletonStore[caches.length];
  int i=0;
  for (  Cache c : caches)   stores[i++]=(SingletonStore)TestingUtil.extractComponent(c,CacheLoaderManager.class).getCacheStore();
  return stores;
}
