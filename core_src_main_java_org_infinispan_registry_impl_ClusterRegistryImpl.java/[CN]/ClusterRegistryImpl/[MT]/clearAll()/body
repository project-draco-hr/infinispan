{
  startRegistryCache();
  Transaction tx=suspendTx();
  try {
    runCommand(new Runnable(){
      @Override public void run(){
        clusterRegistryCache.clear();
      }
    }
);
  }
  finally {
    resumeTx(tx);
  }
}
