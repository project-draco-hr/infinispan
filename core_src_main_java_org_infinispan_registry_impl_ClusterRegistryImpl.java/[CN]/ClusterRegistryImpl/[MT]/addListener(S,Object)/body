{
  startRegistryCache();
  clusterRegistryCache.addListener(listener,new KeyFilter<ScopedKey<S,K>>(){
    @Override public boolean accept(    ScopedKey<S,K> key){
      return key.hasScope(scope);
    }
  }
);
}
