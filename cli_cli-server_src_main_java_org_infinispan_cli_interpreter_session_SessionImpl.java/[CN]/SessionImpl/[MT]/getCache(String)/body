{
  Cache<K,V> c;
  if (cacheName != null) {
    c=cacheManager.getCache(cacheName,false);
  }
 else {
    c=getCurrentCache();
  }
  if (c == null) {
    throw new IllegalArgumentException("No cache named " + cacheName);
  }
  return c;
}
