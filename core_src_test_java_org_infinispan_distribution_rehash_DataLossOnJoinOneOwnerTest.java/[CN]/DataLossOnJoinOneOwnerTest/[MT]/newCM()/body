{
  ConfigurationBuilder c=new ConfigurationBuilder();
  c.clustering().cacheMode(CacheMode.DIST_SYNC).hash().numOwners(1).clustering().l1().disable().locking().supportsConcurrentUpdates(supportsConcurrentUpdates);
  return TestCacheManagerFactory.createClusteredCacheManager(c);
}
