{
  if (!(o instanceof FileEntry))   throw new ClassCastException();
  if (this == o)   return 0;
  FileEntry fe=(FileEntry)o;
  int diff=size - fe.size;
  return (diff != 0) ? diff : offset > fe.offset ? 1 : -1;
}
