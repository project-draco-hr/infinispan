{
  if (isStatsEnabled) {
    if (header.operation == MemcachedOperation.ReplaceIfUnmodifiedRequest) {
      replaceIfUnmodifiedMisses.incrementAndGet();
    }
  }
  if (params == null || !params.noReply)   return NOT_FOUND;
 else   return null;
}
