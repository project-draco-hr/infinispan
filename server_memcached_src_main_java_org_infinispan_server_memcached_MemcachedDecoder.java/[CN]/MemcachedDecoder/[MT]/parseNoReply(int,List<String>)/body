{
  if (args.size() > expectedIndex) {
    if ("noreply".equals(args.get(expectedIndex)))     return true;
 else     throw new StreamCorruptedException("Unable to parse noreply optional argument");
  }
 else   return false;
}
