{
  DummyInMemoryCacheStore cl=new DummyInMemoryCacheStore();
  DummyInMemoryCacheStore.Cfg cfg=new DummyInMemoryCacheStore.Cfg().storeName(DummyInMemoryCacheStoreTest.class.getName());
  cfg.setPurgeSynchronously(true);
  cl.init(cfg,getCache(),getMarshaller());
  cl.start();
  return cl;
}
