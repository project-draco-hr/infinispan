{
  dataContainer.clear();
  try {
    Set<InternalCacheEntry> set=(Set<InternalCacheEntry>)marshaller.objectFromObjectStream(i);
    for (    InternalCacheEntry se : set)     cache.put(se.getKey(),se.getValue(),se.getLifespan(),MILLISECONDS,se.getMaxIdle(),MILLISECONDS,Flag.CACHE_MODE_LOCAL);
  }
 catch (  Exception e) {
    dataContainer.clear();
    throw new StateTransferException(e);
  }
}
