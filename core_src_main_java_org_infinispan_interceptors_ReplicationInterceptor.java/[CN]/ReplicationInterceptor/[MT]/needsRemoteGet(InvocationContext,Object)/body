{
  final CacheEntry entry;
  return !ctx.hasFlag(Flag.CACHE_MODE_LOCAL) && stateTransferManager.isStateTransferInProgressForKey(key) && ((entry=ctx.lookupEntry(key)) == null || entry.isNull() || entry.isLockPlaceholder());
}
