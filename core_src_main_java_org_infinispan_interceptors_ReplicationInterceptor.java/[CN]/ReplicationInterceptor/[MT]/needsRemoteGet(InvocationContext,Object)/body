{
  final CacheEntry entry;
  return !ctx.hasFlag(Flag.CACHE_MODE_LOCAL) && !ctx.hasFlag(Flag.SKIP_REMOTE_LOOKUP) && stateTransferManager.isStateTransferInProgressForKey(key)&& ((entry=ctx.lookupEntry(key)) == null || entry.isNull() || entry.isLockPlaceholder());
}
