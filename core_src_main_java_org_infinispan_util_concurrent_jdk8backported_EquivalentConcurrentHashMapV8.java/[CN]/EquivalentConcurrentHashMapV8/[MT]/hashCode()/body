{
  int h=0;
  Traverser<K,V,Object> it=new Traverser<K,V,Object>(this);
  V v;
  while ((v=it.advance()) != null) {
    h+=keyEquivalence.hashCode(it.nextKey) ^ valueEquivalence.hashCode(v);
  }
  return h;
}
