{
  if (o != null) {
    Iterator<V> it=new ValueIterator<K,V>(map);
    while (it.hasNext()) {
      if (map.valueEquivalence.equals(it.next(),o)) {
        it.remove();
        return true;
      }
    }
  }
  return false;
}
