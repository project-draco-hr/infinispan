{
  return new CloudCacheStoreConfiguration(identity,password,bucketPrefix,proxyHost,proxyPort,requestTimeout,lazyPurgingOnly,cloudService,cloudServiceLocation,maxConnections,secure,compress,purgeOnStartup,purgeSynchronously,purgerThreads,fetchPersistentState,ignoreModifications,TypedProperties.toTypedProperties(properties),async.create(),singletonStore.create());
}
