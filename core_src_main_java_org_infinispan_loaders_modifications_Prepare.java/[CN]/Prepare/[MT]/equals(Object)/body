{
  if (obj == this)   return true;
  if (!(obj instanceof Prepare))   return false;
  Prepare other=(Prepare)obj;
  return list.equals(other.list) && tx.equals(other.tx) && isOnePhase == other.isOnePhase;
}
