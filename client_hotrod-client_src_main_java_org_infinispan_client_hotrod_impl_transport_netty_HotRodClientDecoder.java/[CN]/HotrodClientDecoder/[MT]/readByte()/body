{
synchronized (buffer) {
    if (!buffer.readable()) {
      try {
        buffer.wait();
      }
 catch (      InterruptedException e) {
        throw new TransportException(e);
      }
    }
    return buffer.readUnsignedByte();
  }
}
